# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0.3

# AWS and data lake
boto3>=1.43.36
pyarrow>=12.0.0
awswrangler>=3.0.0  # Lambda uses AWSSDKPandas managed layer; needed locally for scripts

# Database
psycopg2-binary>=2.9.12
duckdb>=1.5.4  # generated by scripts/sync_ducklake_version.py from config/lambda/ducklake/version.yaml; enforced by the ducklake-version-lockstep validate gate
python-ulid>=3.1.0  # monotonic ULID generation for DuckLake SCD2 history PKs (ducklake_runtime.py)
pyiceberg[glue,duckdb]>=0.11.1

# Data ingestion
yfinance>=1.5.1
requests>=2.31.0
beautifulsoup4>=4.15.0

# Machine learning
pysr>=0.16.0
sympy>=1.14.0
torch>=2.0.0
scikit-learn>=1.3.0

# Async and web
aiohttp>=3.14.1

# Code analysis (production -- runs in ops_data_portal for risk scoring)
radon>=6.0.0
networkx>=3.0

# Import governance (Decision 80 / T3.11)
import-linter>=2.0

# Development
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-cov>=4.1.0
pytest-picked>=0.5.1
pytest-randomly>=4.1.0
pytest-socket>=0.8.0
ruff==0.15.20
mypy>=1.5.0
pydantic>=2.0.0
