schema_version: 4
handoff_policy:
  full_validation_required_before_commit: true
  timeout_disposition: blocked
slug: "decision-entry-flow-governance"
intent: >-
  Install the flow-side governance pair on NEW docs/DECISIONS.md entries -- a typed metadata
  envelope, a required Significance routing-row claim, and a per-new-entry byte cap -- so the
  decision corpus's size ceilings pace growth instead of scheduling their own raises, and so the
  sole front-door control on corpus shape stops being self-certified with no record.
plan_type: IMPLEMENTATION
verification_tier: V2
plan_path: docs/plans/PLAN-decision-entry-flow-governance.yaml
phase: >-
  Migration step 2 of audits/contract-first-governance-33c8667.yaml (findings CFG-01, CFG-03,
  CFG-07 plus the 2026-08-07 post-run adversarial-review amendments). Platform tier_item T2.56
  (normative/contract/ADR layering) is the eventual owner of this corpus's shape; this plan closes
  none of its exit criteria and declares no closes_criteria. Step 1 (PLAN-decision-ceiling-bridge,
  PR #863) has landed; step 3 (destination readiness) owns this plan's WARN-to-hard-fail flip.
scope:
  - file: scripts/checks/decisions/_baseline.py
    action: Create
    purpose: >-
      Single shared origin/main baseline reader (decision numbers present at origin/main, None as
      the advisory-skip sentinel), extracted verbatim in behaviour from
      validate_decision_entry_conformance so the size check does not hand-roll a second git read.
      Memoized per root so the two --pre consumers share ONE git invocation (Decision 153 fast-tier
      budget; decision-scout NOTE flag).
  - file: scripts/decisions_md.py
    action: Modify
    purpose: >-
      Add envelope extraction (a fenced ```yaml block immediately following a '## Decision N:'
      heading) and make parse_decisions_md read it FIRST for status / decided_date / amends /
      title_supersedes, falling back to today's bold-marker and title-relation grammar when absent.
      Surface a new `significance` row key. Public helper for the checks to reuse -- never a second
      envelope parser (Decision 134 clause 3 shared-parser mandate).
  - file: scripts/decisions_index.py
    action: Modify
    purpose: >-
      Inherit envelope-sourced amends/supersedes/status/decided_date edges through the shared
      parser rather than re-deriving them; record in the module docstring that `significance` is
      deliberately NOT projected into docs/decisions-index.json (the 131,000-byte pin, Decision 166
      point 9, and the index-only precedent set by intent/amends/title_supersedes).
  - file: scripts/checks/decisions/validate_decision_entry_conformance.py
    action: Modify
    purpose: >-
      Enforce on new-in-diff entries only: (i) a well-formed envelope is present; (ii) its
      `significance` value names one of decision-entry.yaml's four significance.routing tokens plus
      a non-empty one-line justification, and the two rows whose own contract text reads "Never a
      numbered Decision" FAIL; (iii) an envelope field and its bold-marker twin must not carry
      conflicting values; (iv) the envelope's `number` matches the heading number. Reads the token
      vocabulary from the contract at check time, never a hardcoded copy.
  - file: scripts/checks/decisions/validate_decisions_size.py
    action: Modify
    purpose: >-
      Add a forward-only per-NEW-entry byte cap of 6,144 bytes (heading-to-next-heading UTF-8 span)
      landing WARN-TIER -- it prints a named warning and MUST NOT append to `failed`. Historical
      entries are never measured. Uses the shared baseline reader; on an unreachable origin/main the
      per-entry cap advisory-skips while the two existing stock ceilings still run.
  - file: scripts/checks/decisions/validate_supersession_annotations.py
    action: Modify
    purpose: >-
      Union envelope-borne amends/supersedes edges into extract_supersession_edges so an
      envelope-bearing entry cannot silently escape the annotation guard -- _EDGE_RE matches only
      the textual "amends Decision N" prose form, which a typed envelope no longer emits.
      Non-regression obligation, not a feature.
  - file: scripts/checks/registry.py
    action: Modify
    purpose: >-
      Register validate_decision_entry_conformance in the --pre tier, glob-gated on
      docs/DECISIONS.md and docs/DECISIONS_ARCHIVE.md. Today it is full-tier only, so it fires
      post-merge on main and never gates the PR -- CFG-03's stated acceptance ("fails --pre") is
      not achievable without this.
  - file: docs/contracts/decision-entry.yaml
    action: Modify
    purpose: >-
      Add the metadata_envelope section (field list, delimiter, placement, forward-only scope,
      compacted-stub exemption); add the audit's three-question routing rule with its
      standing-commitment clause to the significance section; add the lever-by-ceiling matrix and
      the per-entry cap value to size_governance; add Significance to the required elements for new
      entries. Field semantics only (Decision 86/127 routing) -- no rationale prose here.
  - file: docs/DECISIONS.md
    action: Modify
    purpose: >-
      Add exactly ONE new entry, Decision 167, ratifying the whole ruling (envelope + required
      Significance claim + cap value + routing-rule home). Authored under the 6,144-byte cap,
      carrying its own envelope as the first exemplar, and carrying ruling / rationale / reversal
      conditions / pointers only.
  - file: docs/decisions-index.json
    action: Modify
    purpose: Regenerate via `scripts.decisions_index --write` so the freshness check passes with the Decision 167 row.
  - file: tests/test_decisions_md.py
    action: Modify
    purpose: Envelope precedence, fallback on the grandfathered band, and byte-reconstruction preservation.
  - file: tests/test_decisions_index.py
    action: Modify
    purpose: Envelope-sourced edges reach the index; `significance` is absent from the projection.
  - file: tests/checks/decisions/test_validate_decision_entry_conformance.py
    action: Modify
    purpose: Significance presence, the strict token rejection, envelope/body conflict, number mismatch, historical-band grandfathering.
  - file: tests/checks/decisions/test_validate_decisions_size.py
    action: Modify
    purpose: Per-entry cap WARN-tier semantics -- over-cap new entry warns without failing; historical over-cap entries are silent.
  - file: tests/checks/decisions/test_validate_supersession_annotations.py
    action: Modify
    purpose: Envelope-borne amends/supersedes edges are discovered by the annotation guard.
  - file: tests/checks/decisions/test_baseline.py
    action: Create
    purpose: Shared baseline reader -- reachable branch, unreachable-sentinel branch, and single-read memoization per root.
  - file: tests/test_checks_registry.py
    action: Modify
    purpose: validate_decision_entry_conformance is reachable in the --pre sequence with its DECISIONS glob gate.
bundled_recommendations: []
infrastructure_dependencies: []
acceptance_criteria:
  - >-
    A genuinely-new decision entry lacking a well-formed metadata envelope fails
    `validate --pre`; every one of the 119 grandfathered entries still passes.
  - >-
    A new entry whose envelope `significance` value is absent, is not one of decision-entry.yaml's
    four significance.routing tokens, carries no one-line justification, or names `operational_fact`
    or `field_semantics` fails `validate --pre` with a message quoting the row's own "Never a
    numbered Decision" text and naming the correct destination.
  - >-
    A new entry whose envelope field conflicts with its bold-marker twin (e.g. envelope
    `status: Decided` against `**Status:** Superseded`) fails; agreement or absence of either side
    passes.
  - >-
    A new entry exceeding 6,144 bytes emits a named WARNING from validate_decisions_size and does
    NOT append to `failed`; an existing entry over 6,144 bytes produces no output at all.
  - >-
    parse_decisions_md reads the envelope first and the bold-marker/title grammar as fallback:
    for every decision number present at both origin/main and HEAD, every parsed field is
    byte-identical to its origin/main value.
  - >-
    An envelope-bearing entry declaring `amends` or `supersedes` is visible to
    validate_supersession_annotations' edge extraction (no silent escape of the annotation guard).
  - >-
    validate_decision_entry_conformance appears in the --pre sequence, glob-gated on the two
    DECISIONS files, and is reachable via scripts/validate.py's facade re-export.
  - >-
    docs/contracts/decision-entry.yaml carries the metadata_envelope section, the three-question
    routing rule with its standing-commitment clause, the lever-by-ceiling matrix, and the cap
    value; the four routing tokens the conformance check enforces are read from it, not hardcoded.
  - >-
    Decision 167 exists, is under 6,144 bytes, carries its own envelope, and passes the new
    conformance and cap checks it installs.
  - >-
    `bin/venv-python -m scripts.validate` (full tier) exits 0 on the complete tree.
verification_plan:
  - step: 1
    phase: pre-deploy
    action: >-
      Envelope precedence and fallback -- an entry whose envelope status/date/amends disagree with
      its bold markers and title parenthetical parses to the ENVELOPE values; an entry with no
      envelope parses to the bold-marker/title values unchanged.
    command: bin/venv-python -m pytest tests/test_decisions_md.py -k "envelope" -q
    expected: All envelope precedence and fallback tests pass; no existing test in the file regresses.
    fix_if: >-
      A failure showing bold-marker values winning means the envelope read is layered after the
      fallback instead of before it; a failure on a no-envelope fixture means the fallback path was
      replaced rather than retained.
    hermetic: true
    graduation: graduate
    graduation_check_id: decisions-md-envelope-precedence
  - step: 2
    phase: pre-deploy
    action: >-
      Live-corpus non-regression -- parse every decision present at BOTH origin/main and HEAD under
      the new parser and assert each parsed field is byte-identical to the same entry parsed from
      the origin/main blob. This is the guard that the dual-grammar window changed nothing for the
      119 grandfathered entries.
    command: >-
      bin/venv-python -c "import subprocess,sys,tempfile,pathlib; from scripts.decisions_md import
      parse_decisions_md; d=pathlib.Path(tempfile.mkdtemp());
      [(d/n).write_bytes(subprocess.run(['git','show',f'origin/main:docs/{n}'],capture_output=True).stdout)
      for n in ('DECISIONS.md','DECISIONS_ARCHIVE.md')];
      base={r['decision_id']:r for r in parse_decisions_md([d/'DECISIONS.md',d/'DECISIONS_ARCHIVE.md'])};
      cur={r['decision_id']:r for r in parse_decisions_md()};
      keys=[k for k in ('title','status','problem','intent','decision_text','context','decided_date','related_decisions','reversal_conditions','superseded_by','amends','title_supersedes')];
      bad=[(n,k) for n in sorted(set(base)&set(cur)) for k in keys if base[n][k]!=cur[n][k]];
      print('DRIFT:',bad) or sys.exit(1) if bad else print(f'OK: {len(set(base)&set(cur))} shared entries identical')"
    expected: >-
      Prints "OK: <n> shared entries identical" where n is the count of decision numbers present in
      both snapshots (119 at planning time), and exits 0. No DRIFT line.
    fix_if: >-
      Any DRIFT tuple names an entry/field the envelope logic changed for the grandfathered band --
      the envelope read must be strictly additive and must never fire on an entry with no envelope.
    hermetic: false
    graduation: graduate
    graduation_check_id: decisions-md-historical-band-parse-parity
  - step: 3
    phase: pre-deploy
    action: >-
      Significance enforcement -- required presence, the four-token vocabulary read from the
      contract, the non-empty one-line justification, and the strict rejection of the two
      "Never a numbered Decision" rows on a new entry.
    command: bin/venv-python -m pytest tests/checks/decisions/test_validate_decision_entry_conformance.py -k "significance" -q
    expected: >-
      Presence, unknown-token, empty-justification, `operational_fact` and `field_semantics` cases
      all FAIL the check; `numbered_decision` and `cd_state_flip` pass; historical-band entries
      without the element are untouched.
    fix_if: >-
      An `operational_fact` fixture passing means the strict branch is not wired; a historical-band
      fixture failing means the new-in-diff gate was bypassed and the check is retro-enforcing.
    hermetic: true
    graduation: graduate
    graduation_check_id: decision-entry-significance-strict-token
  - step: 4
    phase: pre-deploy
    action: >-
      Envelope integrity -- envelope/bold-marker conflict on one field fails; agreement passes;
      absence of the bold marker passes; an envelope `number` disagreeing with its heading fails.
    command: bin/venv-python -m pytest tests/checks/decisions/test_validate_decision_entry_conformance.py -k "envelope" -q
    expected: Conflict and number-mismatch cases fail the check; agreement and marker-absent cases pass.
    fix_if: >-
      An agreeing fixture failing means the conflict rule is comparing presence rather than value;
      a conflicting fixture passing means the bold-marker twin is never read for comparison.
    hermetic: true
    graduation: graduate
    graduation_check_id: decision-entry-envelope-integrity
  - step: 5
    phase: pre-deploy
    action: >-
      Per-entry cap WARN-tier semantics -- an over-cap NEW entry produces a named warning and leaves
      `failed` EMPTY; an over-cap HISTORICAL entry produces nothing; a new entry at exactly 6,144
      bytes is silent (boundary).
    command: bin/venv-python -m pytest tests/checks/decisions/test_validate_decisions_size.py -k "per_entry" -q
    expected: >-
      The over-cap new-entry case asserts `failed == []` and a warning string naming the cap and the
      entry; the historical over-cap case asserts no output; the boundary case is silent.
    fix_if: >-
      A non-empty `failed` means the cap was wired hard-fail instead of WARN-tier -- the flip is
      owned by migration step 3, not this plan. Silence on the over-cap new entry means the baseline
      reader is not classifying it as new.
    hermetic: true
    graduation: graduate
    graduation_check_id: decisions-size-per-entry-cap-warn-tier
  - step: 6
    phase: pre-deploy
    action: >-
      Supersession-guard non-regression -- an envelope-bearing entry declaring `amends: [150]` and
      `supersedes: [X]` yields the same edges the textual "amends Decision 150" prose form would.
    command: bin/venv-python -m pytest tests/checks/decisions/test_validate_supersession_annotations.py -k "envelope" -q
    expected: extract_supersession_edges returns the envelope-declared edges; the guard's annotation obligation applies to them.
    fix_if: >-
      Zero edges returned means only _EDGE_RE's prose form is consulted -- the envelope union was
      not wired, and envelope entries silently escape the guard.
    hermetic: true
    graduation: graduate
    graduation_check_id: supersession-envelope-borne-edges
  - step: 7
    phase: pre-deploy
    action: >-
      Shared baseline reader -- one git read per validate run across both consumers (Decision 153
      fast-tier budget), plus the unreachable-origin advisory-skip sentinel.
    command: bin/venv-python -m pytest tests/checks/decisions/test_baseline.py -q
    expected: >-
      The memoization test asserts exactly ONE subprocess git invocation when both checks run
      against one root; the unreachable case returns the None sentinel rather than raising.
    fix_if: >-
      More than one invocation means each check still calls its own reader -- collapse them onto the
      shared module. A raise on the unreachable case breaks the advisory-skip contract.
    hermetic: true
    graduation: graduate
    graduation_check_id: decisions-baseline-reader-single-read
  - step: 8
    phase: pre-deploy
    action: >-
      Contract/check token parity -- the four significance.routing tokens the check enforces are
      exactly the keys in decision-entry.yaml, and the two rejected rows are identified from the
      contract's own "Never a numbered Decision" text rather than a hardcoded list.
    command: bin/venv-python -m pytest tests/checks/decisions/test_validate_decision_entry_conformance.py -k "contract_parity" -q
    expected: >-
      The test derives the token set and the rejected subset from the contract file and asserts the
      check agrees; adding a fifth routing row to the fixture contract changes the accepted set with
      no code edit.
    fix_if: >-
      A divergence means the token list was copied into the module -- the same hardcoded-copy defect
      decision-entry.yaml's shared_parser_note and test_decision_field_grammar.py exist to prevent.
    hermetic: true
    graduation: graduate
    graduation_check_id: decision-entry-contract-token-parity
  - step: 9
    phase: pre-deploy
    action: >-
      Decision 167 self-conformance -- the entry this plan authors is under the cap it installs,
      carries a well-formed envelope, and passes the conformance check as a genuinely-new entry.
    command: >-
      bin/venv-python -c "import sys; from scripts.decisions_md import iter_decision_sections;
      from pathlib import Path;
      b=dict((int(m.group(1)),blk) for m,blk in iter_decision_sections(Path('docs/DECISIONS.md').read_text(encoding='utf-8')))[167];
      n=len(b.encode('utf-8')); fence=chr(96)*3+'yaml'; print(f'D167 {n} B / 6144 cap; envelope={fence in b}');
      sys.exit(0 if n<=6144 and fence in b else 1)"
    expected: >-
      Prints "D167 <n> B / 6144 cap; envelope=True" with n at or under 6144, and exits 0.
    fix_if: >-
      Over the cap -- move specification out to docs/contracts/decision-entry.yaml (the intended
      pressure) or compress the Related pointer block; never split into a sibling entry, which burns
      a second header against the first-binding ceiling.
    hermetic: true
    graduation: waive
    graduation_waiver_reason: >-
      Asserts a property of one specific entry authored by this plan, not a standing repository
      invariant -- graduating it would pin a permanent guard to decision number 167. The general
      property (every new entry under the cap) is graduated at step 5 as
      decisions-size-per-entry-cap-warn-tier.
  - step: 10
    phase: pre-deploy
    action: >-
      Index edges, freshness and projection boundary -- envelope-declared amends/supersedes reach
      the index through the shared parser, the regenerated index matches the committed file, and
      `significance` never appears in it.
    command: >-
      bin/venv-python -m pytest tests/test_decisions_index.py -q &&
      bin/venv-python -m scripts.decisions_index --check && bin/venv-python -c "import json,sys;
      d=json.load(open('docs/decisions-index.json'));
      rows=d['decisions'] if isinstance(d,dict) and 'decisions' in d else d;
      bad=[r for r in (rows.values() if isinstance(rows,dict) else rows) if 'significance' in r];
      print('LEAKED:',len(bad)) or sys.exit(1) if bad else print('OK: significance absent from projection')"
    expected: >-
      The index tests pass (envelope-declared edges present), the freshness check exits 0, and the
      projection carries no `significance` key on any row.
    fix_if: >-
      A freshness failure means `--write` was not re-run after the Decision 167 edit. A leaked key
      means significance was added to the index projection, which grows every future row against the
      131,000-byte pin (Decision 166 point 9).
    hermetic: false
    graduation: graduate
    graduation_check_id: decisions-index-significance-not-projected
  - step: 11
    phase: pre-deploy
    action: >-
      Registry reachability -- validate_decision_entry_conformance is present in the --pre sequence
      with its DECISIONS glob gate and resolves through scripts/validate.py's facade re-export
      (without the re-export line, --pre KeyErrors on the check name).
    command: bin/venv-python -m pytest tests/test_checks_registry.py -q
    expected: >-
      The --pre sequence contains validate_decision_entry_conformance with
      pre_globs=("docs/DECISIONS.md", "docs/DECISIONS_ARCHIVE.md"), and every registered check in
      both tiers resolves to a module-level global in scripts/validate.py.
    fix_if: >-
      A KeyError or reachability failure means the facade re-export line is missing from
      scripts/validate.py -- dispatch is globals()[name](failed), with no auto-discovery.
    hermetic: true
    graduation: graduate
    graduation_check_id: decision-entry-conformance-pre-tier-registered
  - step: 12
    phase: pre-deploy
    action: >-
      Fast tier on the complete tree -- the authoritative pre-merge gate, now including the
      conformance check in its --pre registration.
    command: bin/venv-python -m scripts.validate --pre
    expected: >-
      Exit 0. The DECISIONS size section prints the per-entry cap line with no warning (Decision 167
      is under cap); the decision-entry conformance section reports 1 new entry conforming.
    fix_if: >-
      A conformance failure names the missing element on Decision 167. A size failure means a stock
      ceiling breached -- check header count and combined bytes, never raise the cap to fit.
    hermetic: false
    graduation: not-applicable
  - step: 13
    phase: pre-deploy
    action: Full-tier validation on the complete tree before commit (handoff_policy obligation, Decision 163).
    command: bin/venv-python -m scripts.validate
    expected: Exit 0 across every registered check in the full sequence.
    fix_if: >-
      Triage the named failing check. Do not narrow a check to pass; forward-fix per Decision 55, and
      if the failure is in CI file a source=ci_rca rec rather than patching inline.
    hermetic: false
    graduation: not-applicable
test_obligations:
  - source: scripts/decisions_md.py
    behavior: >-
      The envelope is read before the bold-marker/title grammar for status, decided_date, amends and
      title_supersedes, and the fallback path stays intact for entries with no envelope.
    verification_step: 1
    test_selector: tests/test_decisions_md.py
    red_green_expectation: >-
      Red before -- no envelope parsing exists, so an envelope-bearing fixture returns the
      bold-marker values. Green after.
  - source: scripts/decisions_index.py
    behavior: >-
      Envelope-sourced amends/supersedes edges reach the index through the shared parser, and
      `significance` is never projected into the committed JSON.
    verification_step: 10
    test_selector: tests/test_decisions_index.py
    red_green_expectation: Red before -- envelope-declared edges are invisible to the index. Green after.
  - source: scripts/checks/decisions/validate_decision_entry_conformance.py
    behavior: >-
      A new entry must carry a well-formed envelope whose significance names an accepted routing
      token with a justification; the two "Never a numbered Decision" rows are rejected; envelope/
      body conflicts and number mismatches fail; the historical band is never retro-enforced.
    verification_step: 3
    test_selector: tests/checks/decisions/test_validate_decision_entry_conformance.py
    red_green_expectation: Red before -- the check enforces only Status/Date/Decision markers. Green after.
  - source: scripts/checks/decisions/validate_decisions_size.py
    behavior: >-
      A new entry over 6,144 bytes warns without failing; historical entries are never measured; the
      two existing stock ceilings keep their current fail behaviour.
    verification_step: 5
    test_selector: tests/checks/decisions/test_validate_decisions_size.py
    red_green_expectation: Red before -- no per-entry cap exists, so an over-cap new entry is silent. Green after.
  - source: scripts/checks/decisions/validate_supersession_annotations.py
    behavior: Envelope-borne amends/supersedes edges are extracted alongside the textual prose form.
    verification_step: 6
    test_selector: tests/checks/decisions/test_validate_supersession_annotations.py
    red_green_expectation: Red before -- an envelope-only entry yields zero edges. Green after.
  - source: scripts/checks/decisions/_baseline.py
    behavior: >-
      Returns the origin/main decision-number set, None on unreachable origin, and performs exactly
      one git read per root across both consumers.
    verification_step: 7
    test_selector: tests/checks/decisions/test_baseline.py
    red_green_expectation: Red before -- the module does not exist. Green after.
  - source: scripts/checks/registry.py
    behavior: validate_decision_entry_conformance is present in the --pre sequence, glob-gated on the two DECISIONS files.
    verification_step: 11
    test_selector: tests/test_checks_registry.py
    red_green_expectation: >-
      Red before -- the check is absent from pre_sequence(), so the reachability assertion for the
      --pre tier fails. Green after.
constraints:
  - >-
    FORWARD-ONLY. The 119-entry historical band is never retro-enforced and no ratified body is
    rewritten (Decision 151 precedent; the audit's corpus_shape is
    extract_machine_semantics_only, forward-only). "New" means a decision number absent from the
    origin/main baseline, the same test the conformance check already applies.
  - >-
    The per-entry cap lands WARN-TIER. It prints and MUST NOT append to `failed`. The hard-fail flip
    is owned by migration step 3 (destination readiness) -- landing it hard here is the
    pressure-before-destination mistake the audit's own sequence warns against, since routing is
    one-way-at-entry and no lifecycle mechanism undoes a misroute later.
  - >-
    Cap value is 6,144 bytes (heading-to-next-heading UTF-8 span), ruled by the operator at Step 6b.
    Do not change it during implementation. If Decision 167 will not fit, move specification into
    docs/contracts/decision-entry.yaml -- never raise the cap, and never split into a sibling entry
    (two capped entries burn two headers against the first-binding ceiling).
  - >-
    Exactly ONE new numbered Decision entry. Decision 167 covers the entire ruling (envelope +
    Significance element + cap value + routing-rule home) as a single entry.
  - >-
    Envelope delimiter is a fenced ```yaml block, NOT a `---` fence: `---` already carries a pinned,
    different meaning in this file (decision-entry.yaml block_separator; 99 standalone occurrences).
    Both forms were probed and parse identically -- the choice is legibility, and the fence avoids
    minting a second meaning for the file's own landmark.
  - >-
    Mechanics route to docs/contracts/decision-entry.yaml, rationale to Decision 167 (Decision
    86/127). No new standing prose-architecture doc under docs/ (Decision 86 intent-doc freeze).
  - >-
    `significance` is NOT added to _DECISION_BACKFILL_COLS, DecisionPayload,
    config/agent/data_quality, or docs/decisions-index.json -- it is parser-surfaced and check-read
    only, mirroring the index-only treatment of intent/amends/title_supersedes (Decision 84
    warehouse-safety boundary; Decision 166 point 9 index pin).
  - >-
    Compacted stubs (Decision 149) are EXEMPT from the envelope obligation -- a compaction replaces a
    body it never authored, and its stub_grammar fixes `**Status:** Superseded`, which would
    otherwise trip the envelope/body conflict rule. State the exemption in decision-entry.yaml.
  - >-
    One shared origin/main baseline read across both --pre consumers (Decision 153 fast-tier budget).
    Do not add a second git invocation to the fast tier.
  - >-
    Do not hardcode the routing-token vocabulary. The check reads it from decision-entry.yaml at
    check time, as it already does for required_markers (contract is the single source of truth).
  - No rescue agents or workaround loops (Decision 55). A cap or conformance failure fails loud and is forward-fixed.
  - Only files in the Scope table may be modified.
context:
  - >-
    "Decision-scout verdict + CITE list (verbatim decision ids): FLAGS_FOUND (119 of 119 triaged).
    CITE: Decision 160, Decision 150, Decision 151, Decision 134, Decision 165, Decision 166,
    Decision 104, Decision 84, Decision 86, Decision 127, Decision 128. RELATED: Decision 145,
    Decision 146, Decision 118, Decision 132, Decision 105."
  - "gates: decision-scout=FLAGS_FOUND; plan-critique=<verdict> after <N> round(s)"
  - >-
    Decision-scout flag resolutions, all accepted and folded into this plan -- Decision 163 [WARN]:
    a plan declaring full_validation_required_before_commit must carry a VP step invoking
    `-m scripts.validate` without --pre (verified against validate_handoff_full_tier); step 13 is
    that step. Decision 160 [SPIRIT WARN]: point 11 names the per-entry norm as the only lever that
    bends the accrual RATE, and a WARN-tier cap does not bend it -- Decision 167 clause 3 states the
    WARN tier as a dated pre-commitment with a named flip trigger and explicitly disclaims that the
    lever is installed. Decision 149 [NOTE]: compacted stubs are exempted from the envelope
    obligation. Decision 153 [NOTE]: one shared baseline read, asserted at step 7.
  - >-
    TWO CORRECTIONS TO THE AUDIT'S OWN PARAMETERS, both measured this session. (1) CFG-01's
    acceptance reads "the D164-shaped ruling (3.9 KB) authors cleanly" -- Decision 164 actually
    measures 7,257 bytes; 3,898 is the D61-100 BAND MEDIAN from the adjacent cell of the same table.
    No 4-5 KB cap admits the audit's own clean-ADR exemplar, so the cap value was re-derived from
    measurement rather than inherited. (2) CFG-03's acceptance reads "fails --pre", but
    validate_decision_entry_conformance is registered in the FULL tier only (registry.py:218) while
    PR CI runs --pre (ci.yml:67) and full runs post-merge on main -- the marker would gate nothing at
    PR time without the --pre registration this plan adds.
  - >-
    CAP RULING EVIDENCE (operator ruling at Step 6b: 6,144 bytes). Measured over the live corpus:
    median 4,060 B across 119 entries; last-10 median 6,625 B. Entries over each candidate cap
    (last-10 / last-16 / all-119): 5,120 -> 7/12/41; 6,144 -> 5/10/31; 7,168 -> 5/10/25;
    8,192 -> 3/7/17. 6,144 sits below the last-10 median so it bites the current authoring norm.
    Decision 164's ruling+rationale+reversal+Problem+Intent total 5,290 B; its overrun is a 1,967 B
    Related citation block, so 6,144 admits a D164-shaped ruling while pricing pointer bloat. The
    Decision 167 draft measured 5,974 B, but only after trimming its own Related block -- the cap
    forced the intended compression with 170 B of margin, and 5,120 would have made this
    four-clause ruling unauthorable in one entry, tripping the audit's abort criterion on the first
    entry.
  - >-
    SIGNIFICANCE TOKEN STRICTNESS -- resolved by a Fable advice-consult (fresh context, read-only)
    requested by the operator, who was not convinced this should depart from the audit. Verdict:
    the strict form is audit-COMPATIBLE, not a departure. CFG-03's "names one of the four routing
    rows" and f2's "constrained to the four routing-row tokens" are both VOCABULARY BOUNDS (an upper
    bound on what may appear), never acceptance mandates; nothing in CFG-03, f2, or migration step 2
    states the two confessing tokens must pass. The signal-destruction objection was weighed and
    judged weak here: a deliberate misrouter writes `numbered_decision` under either option, so the
    future WARN-tier consistency lint (claimed row vs measured proxies) loses nothing -- the only
    case that changes is an agent honestly classifying content as field_semantics while instructed
    to mint an entry, which under the permissive form lands permanently in an append-only file and
    under the strict form is corrected at the last mutable moment. All six existing voluntary
    Significance stanzas already claim the numbered_decision row and explicitly negate the other
    three, so false-positive exposure on real entry shapes is zero.
  - >-
    Sequencing prerequisite satisfied: the extractor-grammar plan merged as PR #867
    (feat(decision-extractor-grammar-fixes), CFG-06) with its follow-up in PR #868, so
    decisions_md.py, decision-entry.yaml and decisions_index.py are clean at this branch point.
    Migration step 1 (PLAN-decision-ceiling-bridge, PR #863) also landed.
  - >-
    Corpus headroom at planning time: 119/132 live headers, 685,048/780,000 combined bytes,
    docs/decisions-index.json 110,608/131,000 bytes, docs/contracts/decision-entry.yaml 342/500
    effective lines (Decision 166 contracts class). One new entry plus the contract additions fit
    with room. Decision 166 point 9's "binding within ONE more live entry" note is stale after the
    PR #863 bridge raise.
  - >-
    BUNDLED RECS -- deliberately EMPTY; rec-2934, rec-3015 and rec-3016 were each checked against
    their own acceptance criteria and none is satisfied by this plan, so all three stay open for the
    authoring-surface plan (migration step 4). rec-2934 greps for `body_content` in
    decision-entry.yaml AND `validate_decision_entry_shape` in registry.py -- no check by that name
    exists, and the audit deliberately routes this work into two EXISTING correctly-homed checks;
    minting a third check to satisfy a grep string would be tail-wagging-dog, and the WARN tier means
    "enforce" is genuinely half-delivered. rec-3015 (`why not a contract`) and rec-3016
    (`amendment_forms`) both grep .claude/skills/planning/SKILL.md, which is migration step 4's
    surface, not step 2's -- both currently return 0 matches. No `Resolves:` trailer on the squash
    commit.
  - >-
    NOT a surface retirement, so no stale-reference sweep is owed under the closure obligation: the
    envelope is an additive forward-only grammar whose bold-marker fallback is retained permanently
    for the grandfathered band, not an X -> Y cutover. The dual-grammar window closes only at the
    T1.5 portal cutover (migration step 8), which is CD.17-frozen and out of scope here.
  - >-
    docs/contracts/decision-entry.yaml is NOT a Class A/B/C ritual contract (no top-level
    contract:/class: block), so validate_contract_drift skips it as a free-form registry doc per
    Decision 118. Editing it does not trigger the amendment-log-on-change obligation; the audit's
    CFG-11 tracks that gap and it is out of scope here.
  - >-
    Branch was 0 commits behind origin/main at planning time; no scope-file overlap with main, so
    the Main Divergence Assessment is clean and no rebase deferral is recorded.
pre_implementation_checklist:
  - Branch confirmed not on main
  - docs/PROJECT_CONTEXT.md read
  - >-
    Only the plan-cited decision sections read -- Decisions 84, 86, 104, 127, 128, 134, 145, 146,
    149, 150, 151, 153, 160, 163, 165, 166 (targeted per-entry reads, never the whole file)
  - >-
    audits/contract-first-governance-33c8667.yaml findings CFG-01, CFG-03, CFG-07, the end_state
    routing_rule and forcing_functions blocks, and migration_sequence step 2 read
  - All files in scope located and readable
  - Acceptance criteria understood and verifiable
  - >-
    Re-check the current maximum '## Decision NNN:' header in docs/DECISIONS.md before authoring --
    decision numbers are not reserved at draft time and a concurrent PR may have claimed 167
execution_steps:
  - >-
    Create scripts/checks/decisions/_baseline.py -- move _origin_main_reachable and
    _default_baseline_decision_numbers out of validate_decision_entry_conformance, memoize per root
    so both --pre consumers share one git read, and keep the None-on-unreachable advisory-skip
    sentinel. Re-point the conformance check at it with no behaviour change.
  - >-
    Modify scripts/decisions_md.py -- add envelope extraction for a fenced ```yaml block immediately
    following a '## Decision N:' heading, expose it as a public helper, and make parse_decisions_md
    prefer envelope values for status / decided_date / amends / title_supersedes with the existing
    bold-marker and title-relation grammar as fallback. Add the `significance` row key. Preserve the
    byte-reconstruction invariant asserted in tests/test_decisions_md.py.
  - >-
    Modify scripts/decisions_index.py -- confirm envelope-sourced edges flow through the shared
    parser without a second derivation, and record in the docstring that `significance` is
    deliberately not projected (index pin, Decision 166 point 9).
  - >-
    Modify scripts/checks/decisions/validate_decision_entry_conformance.py -- add the envelope
    well-formedness, number-match, envelope/body conflict, and Significance element checks, reading
    the four routing tokens and the two "Never a numbered Decision" rows FROM the contract at check
    time. New-in-diff entries only; exempt compacted stubs.
  - >-
    Modify scripts/checks/decisions/validate_decisions_size.py -- add the forward-only 6,144-byte
    per-NEW-entry cap at WARN tier via the shared baseline reader. It prints; it must not append to
    `failed`. Leave both stock ceilings' fail behaviour untouched.
  - >-
    Modify scripts/checks/decisions/validate_supersession_annotations.py -- union envelope-borne
    amends/supersedes edges into extract_supersession_edges so envelope entries cannot escape the
    annotation guard.
  - >-
    Modify scripts/checks/registry.py -- add validate_decision_entry_conformance to pre_sequence()
    with pre_globs=("docs/DECISIONS.md", "docs/DECISIONS_ARCHIVE.md"). Confirm the
    scripts/validate.py facade re-export line already exists (it does) so dispatch resolves.
  - >-
    Modify docs/contracts/decision-entry.yaml -- add metadata_envelope (fields, ```yaml fence
    delimiter, placement immediately after the heading, forward-only scope, compacted-stub
    exemption); add Significance to the required elements for new entries; add the three-question
    routing rule with the standing-commitment clause to significance:; add the lever-by-ceiling
    matrix and per_entry_max_bytes 6144 to size_governance:. Keep additions terse -- 158 effective
    lines of headroom against the 500-line contracts class, and Decision 128 says compact, never raise.
  - >-
    Author Decision 167 in docs/DECISIONS.md -- one entry, under 6,144 bytes, carrying its own
    ```yaml envelope as the first exemplar, with ruling / rationale / reversal conditions / pointers
    only. Clause 3 must state the WARN tier as a dated pre-commitment naming migration step 3 as the
    flip trigger and disclaim that the Decision 160 point 11 accrual-rate lever is installed.
    Reversal conditions carry the retune/abort trigger (>2 of the first 10 new entries unauthorable
    without losing DECISION content), the same-PR split-evasion review trigger, T1.5 retirement, and
    the never-raise-to-fit pre-commitment in Decision 166 point 6 / Decision 145 form.
  - "Regenerate the index: bin/venv-python -m scripts.decisions_index --write"
  - >-
    Write the tests named in the Scope table and in test_obligations -- each must fail against the
    pre-change tree and pass after (red/green per row).
  - >-
    Execute the Verification Plan -- run each step in order; loop until pass. Step 13 (full tier)
    must complete exit 0 before commit per handoff_policy. On unrecoverable failure stop and RCA
    (Decision 55); never narrow a check to make it pass.
  - >-
    Report: what was implemented, the measured Decision 167 byte size against the cap, the per-entry
    cap warning behaviour observed, and full verification results.
rollback: >-
  Every change is additive and forward-only. Reverting the commit restores the prior grammar with no
  data migration: the envelope is read-preferred but never required by the parser (the bold-marker
  fallback is retained), no ratified body is rewritten, and the only corpus change is the single
  Decision 167 entry plus the regenerated index. If the cap proves mis-set in production, it is a
  one-constant change in validate_decisions_size.py at WARN tier, which fails nothing while it is
  retuned -- the reason the audit staged it WARN-first.
