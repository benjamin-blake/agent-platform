# Plan

## Intent
Complete the T2.18 DuckLake maintenance pipeline (FP-B slice) so the operational lakehouse has durable
catalog disaster-recovery, real alerting on its safety alarms, and a small-file co-tuning mechanism --
the remaining CD.34 exit criteria the FP-A slice deferred. This hardens the storage + durability
substrate the T2.19 ops write/read cutover depends on.

## Plan Type
IMPLEMENTATION

## Verification Tier
V3

## Plan Path
docs/plans/PLAN-ducklake-maintenance-fpb.md

## Phase
Phase 2 (Platform) -- T2 tier, DuckLake operational lakehouse. Tier item T2.18
("DuckLake maintenance pipeline replacing ops_compaction"), FP-B slice. FP-A landed + merged 2026-06-07
(PR #93). T2.18 depends_on [T2.16, T2.17] (both complete); FP-B unblocks the remaining T2.18 exit criteria.

## Scope
| File | Action | Purpose |
|------|--------|---------|
| src/common/catalog_dr.py | Create | Testable catalog-DR primitives: build the `pg_dump --format=custom --serializable-deferrable` command, the engine-version-tagged S3 key + object metadata, the success-metric payload, and a `run_catalog_dump` orchestrator. Loud-fail (raise) on non-zero pg_dump exit -- never emit the success metric on a failed dump. |
| src/lambdas/ducklake_catalog_dr/__init__.py | Create | Package marker. |
| src/lambdas/ducklake_catalog_dr/handler.py | Create | DR Lambda entrypoint: runtime-fetch the Neon DSN (Secrets Manager, Decision 37), invoke `/opt/bin/pg_dump` (from the pgclient layer), upload the engine-version-tagged dump to the DR bucket, emit `CatalogDumpSuccess` to CloudWatch. Accepts `force_*` event fields (Lambda convention). Maps loud-fail to 5xx. |
| src/lambdas/ducklake_catalog_dr/manifest.yaml | Create | CD.24 per-Lambda manifest (`status: active`): includes `src/common/catalog_dr.py` + `scripts/aws_profile.py`; deps minimal (boto3 only -- pg_dump is the pgclient LAYER, not pip). |
| terraform/personal/ducklake_catalog_dr.tf | Create | DR Lambda (from S3) + `ducklake-pgclient` layer version + write-scoped exec role (Neon DSN read, DR-bucket Put/List, `DuckLakeCatalogDR` PutMetricData, logs) + dedicated versioned + SSE + public-access-block + lifecycle (30d expiry, noncurrent + abort-incomplete-multipart cleanup) S3 bucket + EventBridge `cron(0 3 * * ? *)` rule/target/permission + log group + Function URL (AWS_IAM, smoke ingress) + `>25h` freshness `aws_cloudwatch_metric_alarm` -> shared SNS topic. |
| terraform/personal/sns_alerts.tf | Create | One shared personal-account `aws_sns_topic` (`agent-platform-alerts`) + email `aws_sns_topic_subscription` (endpoint from a gitignored tfvars var, NOT hardcoded). Output the topic ARN for both alarms. (Decision 39 -- SNS is the canonical alarm primitive.) |
| terraform/personal/ducklake_maintenance.tf | Modify | (1) Wire the existing circuit-breaker alarm `alarm_actions`/`ok_actions` to the shared SNS topic (remove the `alarm_actions = []` FP-B markers). (2) Add a 3rd EventBridge rule (`hot_merge`) + target (`input = {action="hot_merge"}`) + permission. (3) Add tunable-threshold env vars (`GC_BREAKER_FILE_FRACTION`, `GC_BREAKER_BYTES`) to the maintenance Lambda. |
| src/common/ducklake_maintenance.py | Modify | Add a `HOT_TABLE_SCOPE` constant (smoke-scoped, T2.19 forward-pointer) + a `run_hot_merge` orchestrator (higher-frequency `merge_adjacent_files` over high-write tables, no GC). Source `GC_BREAKER_FILE_FRACTION`/`GC_BREAKER_BYTES` defaults from env when set (tunable knob; FP-A defaults retained -- NOT a Decision-55 relaxation). |
| src/lambdas/ducklake_maintenance/handler.py | Modify | Add `action="hot_merge"` dispatch; read breaker thresholds from env and pass to `run_gc`. |
| scripts/build_lambda.py | Modify | (1) Add `build_pgclient_layer` (stage pg_dump 16 + libpq + deps under `/opt/bin` + `/opt/lib`, mirroring `build_ducklake_extensions_layer`). (2) Build + upload `ducklake-catalog-dr.zip` and the pgclient layer in the `--ducklake-only` path. (3) Add `ducklake_catalog_dr` to the function/deploy maps. |
| scripts/ducklake_neon_smoke_test.py | Modify | Add `--lambda-catalog-dr` (invoke DR; assert a dump object lands in the DR bucket + `CatalogDumpSuccess` metric emitted + object carries the engine-version tag) and `--lambda-maintenance-hot-merge` (invoke hot_merge; assert files merged, no GC/deletion) post-deploy gates. |
| src/data/handlers/ops_compaction_handler.py | Modify | Deprecation marker ONLY (module docstring note + comment): "DEPRECATED -- live Iceberg ops write path; retired at the T2.19 cutover when the DuckLake writer replaces it (Decision 78 clause 7). Do NOT disable before T2.19." No behaviour change. |
| src/lambdas/ops-compaction/manifest.yaml | Modify | Add a `notes` deprecation line pointing to the T2.19-gated decommission runbook. No status change (stays `active`). |
| tests/test_catalog_dr.py | Create | Unit tests for the DR primitives: pg_dump command flags (`--format=custom`, `--serializable-deferrable`), S3 key + engine-version metadata, success-metric payload, and loud-fail-on-nonzero-exit (mocked subprocess + boto -- real code paths). |
| tests/test_ducklake_maintenance.py | Modify | Add coverage for `run_hot_merge` (merge-only, no destructive calls) and env-sourced breaker thresholds. |
| tests/test_ducklake_maintenance_handler.py | Modify | Add `action="hot_merge"` dispatch coverage + env-threshold pass-through to `run_gc`. |
| docs/runbooks/ducklake-catalog-operations.md | Modify | Add: the catalog-DR section (cadence, dump format, restore drill pointer, freshness-alarm reading), the SNS alert wiring, the co-tuning knobs + T2.19 numeric-tuning forward-pointer, and the T2.19-gated ops_compaction decommission runbook. |
| docs/ROADMAP-PLATFORM.yaml | Modify | T2.18 `progress_note`: record FP-B landing + which exit criteria it closes (DR, SNS, co-tune mechanism); note that ops_compaction retirement + final telemetry-tuning numbers formally carry to T2.19. Do NOT set `status: complete`. |
| docs/SESSION_LOG.md | Modify | Session entry. |

## Bundled Recommendations
None. (Open recs surfaced at preflight are not specific to catalog DR / alerting; none bundled. 359 open / 303 non-automatable -- soft-cap breached, informational under Decision 73.)

## Infrastructure Dependencies
| Item | Detail |
|------|--------|
| New resources (DR) | `aws_lambda_function.ducklake_catalog_dr`, `aws_lambda_layer_version.ducklake_pgclient`, `aws_iam_role.ducklake_catalog_dr` + inline policy, `aws_cloudwatch_log_group.ducklake_catalog_dr`, `aws_s3_bucket.ducklake_catalog_dr` + `_versioning` + `_server_side_encryption_configuration` + `_public_access_block` + `_lifecycle_configuration`, `aws_cloudwatch_event_rule.ducklake_catalog_dr` (`cron(0 3 * * ? *)`) + target + `aws_lambda_permission`, `aws_lambda_function_url.ducklake_catalog_dr` (AWS_IAM), `aws_cloudwatch_metric_alarm.ducklake_catalog_dr_freshness` (>25h, `treat_missing_data=breaching`). |
| New resources (SNS) | `aws_sns_topic.alerts`, `aws_sns_topic_subscription.alerts_email` (endpoint via gitignored tfvars var `alerts_email`). |
| Modified resources | `aws_cloudwatch_metric_alarm.ducklake_maintenance_breaker` (`alarm_actions`/`ok_actions` -> SNS topic ARN); `aws_lambda_function.ducklake_maintenance` (+ breaker-threshold env vars); new `aws_cloudwatch_event_rule.ducklake_maintenance_hot_merge` + target + permission. |
| Apply posture | **HUMAN-GATED** via `agent_platform_admin` (Decision 35 + 77). The new IAM role/trust + new S3 bucket trip the deterministic fail-closed guard (`scripts/terraform_apply_guard.py`), routing to the manual admin path, NOT push-to-main auto-apply. SNS topic + email subscription are not IAM-sensitive but ride the same human-gated apply. |
| IAM precedence | IAM (DR role + policy) must be applied BEFORE the DR code deploy: (1) `build_lambda --ducklake-only` uploads the catalog-dr zip + pgclient layer to S3, (2) `terraform plan` -> human review -> apply via `agent_platform_admin`, (3) `build_lambda --ducklake-only --deploy` updates the function code. (terraform CLAUDE.md IAM-precedence.) |
| Lambda deployment (CD.16 / Decision 79) | `ducklake_catalog_dr` is a NEW `status: active` artifact -> V3 per-Lambda build + deploy + smoke-test. `ducklake_maintenance` is MODIFIED -> re-triggers its own V3 gate (re-deploy + re-smoke hot_merge). New `ducklake-pgclient` layer builds alongside the existing `ducklake-deps`/`ducklake-extensions` layers. No model IDs touched (deterministic pg_dump + SQL, no LLM) -> inference-provider validation N/A. |
| Timing | Terraform create + EventBridge rules + bucket + SNS = `[pre-deploy]` (apply). Live invoke gates (DR dump, hot_merge, SNS subscription-confirmation) = `[post-deploy]`. |

## Acceptance Criteria
- [ ] A new `ducklake_catalog_dr` Lambda runs `pg_dump --format=custom --serializable-deferrable` against the Neon catalog (DSN runtime-fetched from Secrets Manager, Decision 37), on EventBridge `cron(0 3 * * ? *)`, and uploads the dump to a dedicated versioned + lifecycle-managed S3 bucket (SSE, public-access-block, 30-day expiry tunable to 7). (CD.34 O-2)
- [ ] Each DR object is engine-version-tagged (PG16 + the pinned DuckLake/DuckDB 1.5.3) in both the S3 key and object metadata, so a restore can be matched to a compatible engine (CD.34 O-2 / OQ.12 re-baseline note).
- [ ] A `>25h` freshness alarm (`CatalogDumpSuccess` metric, namespace `DuckLakeCatalogDR`, `treat_missing_data=breaching`) fires when a daily dump is missed, and pages via the shared SNS topic.
- [ ] One shared personal-account SNS topic exists and is the `alarm_actions` target for BOTH the FP-A circuit-breaker alarm (no longer `alarm_actions = []`) AND the DR freshness alarm. The email subscription endpoint comes from a gitignored tfvars var, not source. (Decision 39)
- [ ] A `hot_merge` higher-frequency cadence exists (EventBridge rule + `action=hot_merge` dispatch + `run_hot_merge` orchestrator) running `merge_adjacent_files` only (no snapshot expiry / file deletion) over a `HOT_TABLE_SCOPE`. The GC circuit-breaker thresholds are env-configurable on the maintenance Lambda. Scope is `ducklake_smoke_*`; a code-level + runbook forward-pointer states the numeric tuning + real high-write-rate tables are wired at T2.19. (CD.34)
- [ ] Co-tuning is a tunability MECHANISM, not a relaxation: the FP-A breaker defaults (>20% files / >10 GiB, 30d/7d/floor-2/>=7d grace) remain the shipped defaults; changing them to make a gate pass is forbidden (Decision 55).
- [ ] `ops_compaction` is UNCHANGED behaviourally (only a deprecation marker + manifest note added); a T2.19-cutover-gated decommission runbook documents the disable/removal sequence. It stays the live Iceberg ops write path until T2.19 (Decision 78 clause 7 / Decision 70 Single-Portal invariant).
- [ ] Per-Lambda V3 build + deploy + smoke-test pass for `ducklake_catalog_dr` (new) and re-pass for `ducklake_maintenance` (modified): a live dump lands in the DR bucket, and a live `hot_merge` invoke merges files without deleting any (Decision 79).
- [ ] T2.18 roadmap item carries an FP-B `progress_note` and remains open (`status: in_progress` / not complete) -- ops_compaction retirement + final telemetry-tuning numbers carry to T2.19.

## Verification Plan
| # | Phase | Action | Command | Expected Outcome | Fix If |
|---|-------|--------|---------|-------------------|--------|
| 1 | [pre-deploy] | Unit-test the DR primitives (pg_dump flags, S3 key + engine tag, metric payload, loud-fail on nonzero exit) on real code paths (mocked subprocess + boto) | `bin/venv-python -m pytest tests/test_catalog_dr.py -q` | All pass; a nonzero pg_dump exit RAISES and emits NO success metric | Missing `--serializable-deferrable`/`--format=custom`, or success metric emitted on failure -> fix the primitive |
| 2 | [pre-deploy] | Unit-test the co-tuning mechanism (`run_hot_merge` merge-only; env-sourced breaker thresholds) | `bin/venv-python -m pytest tests/test_ducklake_maintenance.py tests/test_ducklake_maintenance_handler.py -q` | All pass; `run_hot_merge` issues no destructive call; env thresholds flow into `run_gc` | hot_merge calls expire/cleanup/delete -> remove; env not honoured -> fix the read |
| 3 | [pre-deploy] | Validate both manifests + bundle completeness | `bin/venv-python -m scripts.lambda_manifest --validate && bin/venv-python -m scripts.lambda_manifest --check-bundles` | `ducklake_catalog_dr` validates + bundles; `ducklake_maintenance` re-validates | Missing include -> add to manifest `includes` |
| 4 | [pre-deploy] | Confirm `compute_affected_artifacts` flags both active artifacts | `bin/venv-python -c "from scripts.lambda_manifest import compute_affected_artifacts; print(compute_affected_artifacts(['src/lambdas/ducklake_catalog_dr/handler.py','src/common/catalog_dr.py','src/common/ducklake_maintenance.py']))"` | Output includes `ducklake-catalog-dr` AND `ducklake-maintenance` | Manifest not discovered -> fix path/status |
| 5 | [pre-deploy] | Terraform validate + plan all new/modified resources | `terraform -chdir=terraform/personal validate && terraform -chdir=terraform/personal plan` | Plan adds DR Lambda + pgclient layer + DR bucket (versioned/SSE/PAB/lifecycle) + SNS topic + subscription + 2 alarms wired to SNS + hot_merge rule; modifies breaker alarm `alarm_actions`; NO destroys; present to human | Validate error / unexpected destroy -> stop (Decision 77 guard) |
| 6 | [pre-deploy] | Full presubmit (lint/format/tests/prompts/schema) | `bin/venv-python -m scripts.validate` | PASS (CI-identical suite) | Any failure -> fix before deploy; CI is authoritative |
| 7 | [pre-deploy] | Build the DR zip + pgclient layer (+ existing ducklake artifacts) | `bin/venv-python -m scripts.build_lambda --ducklake-only` | `ducklake-catalog-dr.zip` + `ducklake-pgclient-layer.zip` built + uploaded to S3 | Build error -> fix `build_pgclient_layer` / the `--ducklake-only` extension |
| 8 | [pre-deploy] | HUMAN-GATED apply (IAM + bucket before code) | `terraform -chdir=terraform/personal apply` (via `agent_platform_admin`, after human review of step 5) | Resources created; DR role + bucket + SNS live; breaker alarm now -> SNS | Apply fails on guard -> route through admin path; partial failure -> re-plan |
| 9 | [post-deploy] | Confirm the SNS email subscription (one-time, manual click in the email) then verify it is confirmed | `aws sns list-subscriptions-by-topic --topic-arn $(terraform -chdir=terraform/personal output -raw alerts_topic_arn) --profile agent_platform` | Subscription `SubscriptionArn` is a real ARN (not `PendingConfirmation`) | Still pending -> click the confirmation email; alarms cannot page until confirmed |
| 10 | [post-deploy] | Deploy the function code (DR + re-deploy maintenance) | `bin/venv-python -m scripts.build_lambda --ducklake-only --deploy` | `agent-platform-ducklake-catalog-dr` + `agent-platform-ducklake-maintenance` code updated from S3 | Deploy error -> check function name map in build_lambda |
| 11 | [post-deploy] | Live DR gate: invoke the DR Lambda; assert a dump object lands in the DR bucket, engine-version-tagged, and the success metric is emitted | `bin/venv-python -m scripts.ducklake_neon_smoke_test --lambda-catalog-dr` | A `.dump` object appears in the DR bucket; key + metadata carry PG16 + duckdb 1.5.3; `CatalogDumpSuccess=1` emitted; `pg_dump` ran from the layer | pg_dump not found / version mismatch -> fix the pgclient layer (binary + LD_LIBRARY_PATH); no object -> fix the S3 upload |
| 12 | [post-deploy] | Live hot_merge gate: invoke `action=hot_merge`; assert files merged, nothing deleted | `bin/venv-python -m scripts.ducklake_neon_smoke_test --lambda-maintenance-hot-merge` | `files_after <= files_before`; no `cleanup_old_files`/`delete_orphaned_files` issued; `ok=true` | hot_merge deletes / errors -> fix `run_hot_merge` (merge-only) |
| 13 | [post-deploy] | Confirm DR schedule + freshness alarm wiring | `aws events list-rules --name-prefix agent-platform-ducklake-catalog-dr --profile agent_platform && aws cloudwatch describe-alarms --alarm-names ducklake-maintenance-circuit-breaker ducklake-catalog-dr-freshness --query 'MetricAlarms[].AlarmActions' --profile agent_platform` | DR rule ENABLED (`cron(0 3 * * ? *)`); BOTH alarms list the shared SNS topic ARN in AlarmActions | Rule disabled / empty AlarmActions -> fix terraform |
| 14 | [post-deploy] | Confirm ops_compaction untouched (behaviour-neutral marker only) | `aws lambda get-function --function-name agent-platform-ops-compaction --profile agent_platform --query 'Configuration.LastModified'` | LastModified unchanged from before this work (still deployed, still the live Iceberg write path) | If modified -> revert; ops_compaction must stay live until T2.19 |

## Constraints
- No rescue agents or workaround loops (Decision 55). If a live VP step fails unrecoverably, STOP and root-cause -- do not relax a guardrail constant or weaken a DR flag to make a gate pass.
- Breaker + retention constants are CD.33 / Decision 81 invariants. Making them env-configurable is a tunability mechanism; the FP-A defaults remain shipped, and tuning to pass a gate is a Decision-55 silent-degrade and is forbidden.
- pg_dump major version MUST match the Neon server (Postgres 16). Verify `pg_dump --version` in the deployed Lambda sandbox (VP 11) -- a version mismatch produces an unrestorable or refused dump.
- DuckDB pinned 1.5.3 / DuckLake v1.0 lockstep (OQ.12). On any engine bump, re-baseline the DR retention window (CD.33 O-2 as amended by CD.34) -- documented in the runbook.
- ops_compaction is the LIVE Iceberg ops write path. WS4 is behaviour-neutral (marker + runbook only). Do NOT disable, re-schedule, or remove it -- that is a T2.19-cutover action (Decision 78 clause 7 / Decision 70 Single-Portal invariant).
- Terraform apply is human-gated (Decision 35 + 77). The fail-closed guard must remain fail-closed; do not bypass.
- Only modify files in the Scope table. Out-of-scope bugs become recommendations via `scripts/ops_data_portal.py`.
- No emojis; ASCII hyphens only; ruff line length 127; type hints; `bin/venv-python` for all Python.

## Context
- **FP-A baseline (on main):** PR #93 (`0bdd34b`) landed the `ducklake_maintenance` singleton Lambda (daily merge `cron(0 4)`, weekly guarded GC `cron(0 5 ? * SUN)`, circuit breaker, `reserved_concurrent_executions=1`), its IAM role, the breaker `aws_cloudwatch_metric_alarm` with `alarm_actions = []`, the CD.24 manifest, `build_lambda --ducklake-only` extended to 3 functions, and the `--lambda-maintenance-{merge,gc,breaker}` smoke gates. `terraform/personal/ducklake_maintenance.tf` already carries `# FP-B:` markers at the `alarm_actions` lines -- the wiring points are pre-staged. **FP-A is merged but NOT yet deployed** (terraform apply + lambda deploy are human-gated and pending); the FP-B apply (step 8) covers FP-A + FP-B terraform together.
- **Decisions this plan cites** (from the decision-scout gate, FLAGS_FOUND -- one NOTE, no BLOCK):
  - **Decision 82** -- ratifies CD.34 (Neon catalog migration): makes DR-as-pg_dump-to-S3, inlining-disabled-for-all-tables, and "T2.18 co-tunes the GC breaker" enactable. Basis for WS1 + WS3.
  - **Decision 81** (ratifies CD.33) -- maintenance architecture FP-B extends: singleton, two cadences, guarded GC, retention floor, breaker. WS3's hot_merge + env thresholds stay inside clause 5/6 guardrails.
  - **Decision 78 clause 7 / Decision 70** (Single-Portal invariant) -- the NOTE flag: ops_compaction is the live Iceberg write transport preserved until T2.19. WS4 must stay behaviour-neutral (marker + runbook only).
  - **Decision 79** (CD.16 + CD.24) -- per-Lambda manifest + V3 build/deploy/smoke for the new active DR artifact + the modified maintenance artifact + the new pgclient layer.
  - **Decision 77** -- new IAM role + new bucket trip the fail-closed guard -> human-gated admin apply.
  - **Decision 62 / CD.29** -- EventBridge-cron DR Lambda + hot_merge cadence are the inspectable Lambda mechanism, over a new GH Actions surface for non-CI scheduled work.
  - **Decision 49 / Decision 37** -- zip + pip-into-layer is the established packaging precedent; the new pgclient layer + zip DR Lambda follow it (no container-image decision forces otherwise). The DR Lambda runtime-fetches the DSN from Secrets Manager (Decision 37).
  - **Decision 48** -- V3 tier (new Lambda + Terraform + live invoke gates).
  - **Decision 39** -- SNS is the project's canonical alarm/failure-notification primitive; WS2's shared topic introduces it under that precedent (no new decision needed).
  - **Decision 35** -- apply never automatic; the IAM + bucket apply rides the manual path.
  - **Decision 55** -- loud-fail; breaker/DR knobs are tunable but never relaxed to pass a gate.
- **Best-practice DR mechanism (human-delegated decision):** a dedicated zip DR Lambda + a new `ducklake-pgclient` layer (pg_dump 16 + libpq), chosen over a container-image Lambda to stay within the repo's single established packaging paradigm (it already builds `ducklake-deps`/`ducklake-extensions` layers) rather than introduce ECR + a docker build pipeline for one small job. `pg_dump --format=custom` (compressed, supports selective/parallel restore) over plain SQL; `--serializable-deferrable` for a clean non-blocking consistent snapshot.
- **Telemetry co-tuning is a MECHANISM only this slice (human-confirmed):** real telemetry/ops tables only land in DuckLake at the T2.19 cutover (Iceberg until then), so there is no real dead-file rate to tune the breaker against yet. FP-B lands the hot_merge cadence + env-configurable thresholds (smoke-scoped) with the numeric tuning + real high-write tables wired at T2.19 -- mirroring FP-A's `GC_TABLE_SCOPE` smoke-scope + forward-pointer pattern.
- **ops_compaction retirement is STAGED + T2.19-gated (human-confirmed):** investigation found `ops_compaction_handler.py` is the live Iceberg ops WRITE path (S3 ObjectCreated on `staging/` -> `OpsWriter.compact()` -> Iceberg; every `file_rec`/`update_rec` depends on it), and T2.19 IS the write-path migration that replaces it. Disabling it now would break all ops writes. FP-B delivers the decommission runbook + deprecation markers + a T2.19-cutover-gated disable step; the actual schedule-disable/removal executes at T2.19 once the DuckLake writer is proven.
- **Known gotcha (carried from FP-A):** `expire_snapshots` does NOT delete S3 objects; only the GC cadence (cleanup + delete_orphaned) reclaims storage. hot_merge is merge-only and reclaims nothing -- it bounds small-file COUNT between GC passes, not storage. VP 12 asserts hot_merge deletes nothing.
- **Preflight:** branch `claude/platform-roadmap-plan-Z5kYz`, main 0/0 (rebased onto `origin/main` which carries FP-A); creds OK (S3-reader ACCESS_DENIED warnings are the known Iceberg-metadata read fallback to Athena); ci_rca=0 (no block); non-automatable soft-cap breached (303, informational under Decision 73). T2.18 is `in_progress` so not in `next_eligible`; this session is an explicit human-directed continuation (`user_explicit_out_of_scope`).

## Pre-Implementation Checklist
- [ ] Branch confirmed not on `main` (on `claude/platform-roadmap-plan-Z5kYz` or the implement session's harness branch)
- [ ] docs/PROJECT_CONTEXT.md read
- [ ] DECISIONS.md read (esp. 82, 81, 78, 79, 77, 62, 49, 48, 39, 37, 35, 55)
- [ ] All files in Scope table located and readable (FP-A's `ducklake_maintenance.tf` + `ducklake_maintenance.py` + `neon_ducklake_catalog.tf` + `build_lambda.py` layer builders are the templates to mirror)
- [ ] Acceptance Criteria understood and verifiable
- [ ] `pg_dump` major version (16) confirmed matches the Neon server; layer staging verified for the Lambda runtime (AL2023/python3.12/x86_64)

## Ordered Execution Steps
1. **`src/common/catalog_dr.py`** -- DR primitives: `build_pg_dump_cmd(dsn, out_path)` (`pg_dump --format=custom --serializable-deferrable --file=<out> "<dsn>"`), `build_dr_key(now, pg_version, duckdb_version)` (engine-version-tagged key) + the matching object-metadata dict, the `CatalogDumpSuccess` metric payload, and `run_catalog_dump(...)` orchestrating fetch-DSN -> pg_dump (subprocess, check returncode) -> S3 upload -> emit metric. Raise `CatalogDrError` (loud-fail) on any non-zero pg_dump exit BEFORE emitting the success metric.
2. **`src/lambdas/ducklake_catalog_dr/{__init__.py,handler.py}`** -- handler: runtime-fetch the DSN (Secrets Manager `ducklake-neon-catalog-dsn`, Decision 37), call `run_catalog_dump`, accept `force_*` event fields, map loud-fail to 5xx (mirror `ducklake_writer/handler.py`).
3. **`src/lambdas/ducklake_catalog_dr/manifest.yaml`** -- `status: active`, `artifact: ducklake-catalog-dr.zip`, functions `[agent-platform-ducklake-catalog-dr]`, includes `src/common/catalog_dr.py` + `scripts/aws_profile.py`.
4. **`scripts/build_lambda.py`** -- add `build_pgclient_layer` (stage pg_dump 16 + libpq.so + dependent libs under `python`/`/opt/bin`+`/opt/lib`, mirroring `build_ducklake_extensions_layer`); build + upload `ducklake-catalog-dr.zip` + `ducklake-pgclient-layer.zip` in `_run_ducklake_build`; add `ducklake_catalog_dr` to `_DUCKLAKE_FUNCTION_ZIP_KEYS` / the `only_ducklake` deploy set + S3-key map.
5. **`src/common/ducklake_maintenance.py`** -- add `HOT_TABLE_SCOPE` (smoke-scoped + T2.19 forward-pointer) + `run_hot_merge(con, tables=HOT_TABLE_SCOPE)` (flush no-op + merge only; returns files_before/after). Allow `GC_BREAKER_FILE_FRACTION`/`GC_BREAKER_BYTES` defaults to be overridden from env (keep the module constants as the fallback defaults).
6. **`src/lambdas/ducklake_maintenance/handler.py`** -- add `action="hot_merge"` -> `run_hot_merge`; read breaker thresholds from env and pass to `run_gc`.
7. **`terraform/personal/sns_alerts.tf`** -- `aws_sns_topic.alerts` (`agent-platform-alerts`) + `aws_sns_topic_subscription.alerts_email` (protocol `email`, endpoint `var.alerts_email`); declare `var.alerts_email` (no committed default -- supplied via gitignored `terraform.personal.tfvars`); output `alerts_topic_arn`.
8. **`terraform/personal/ducklake_catalog_dr.tf`** -- DR bucket (`aws_s3_bucket` + versioning + SSE + public-access-block + lifecycle 30d/noncurrent/abort-multipart), exec role + inline policy (`secretsmanager:GetSecretValue` on the Neon catalog secret ARN; <!-- pragma: allowlist secret -->  S3 Put/List on the DR bucket; `cloudwatch:PutMetricData` namespace-scoped to `DuckLakeCatalogDR`; logs), log group, `aws_lambda_layer_version.ducklake_pgclient`, Lambda (from S3, pgclient layer attached, `DUCKLAKE_*`/DR-bucket env), Function URL (AWS_IAM), EventBridge `cron(0 3 * * ? *)` rule + target + permission, freshness `aws_cloudwatch_metric_alarm` (`CatalogDumpSuccess` Sum < 1 over a 25h window, `treat_missing_data=breaching`, `alarm_actions=[aws_sns_topic.alerts.arn]`), outputs (function URL + name + bucket).
9. **`terraform/personal/ducklake_maintenance.tf`** -- wire the breaker alarm `alarm_actions`/`ok_actions` to `aws_sns_topic.alerts.arn` (remove the `[]` + FP-B markers); add the `hot_merge` EventBridge rule + target (`{action="hot_merge"}`) + permission; add `GC_BREAKER_FILE_FRACTION`/`GC_BREAKER_BYTES` env vars to the maintenance Lambda.
10. **`src/data/handlers/ops_compaction_handler.py`** + **`src/lambdas/ops-compaction/manifest.yaml`** -- add the deprecation marker (docstring/comment) + manifest `notes` line, pointing to the T2.19-gated decommission runbook. No behaviour change.
11. **`scripts/ducklake_neon_smoke_test.py`** -- add `--lambda-catalog-dr` (SigV4-invoke the DR Function URL; assert dump object + engine-tag + metric) and `--lambda-maintenance-hot-merge` (assert merge-only, nothing deleted) gates.
12. **`tests/test_catalog_dr.py`** (new) + **`tests/test_ducklake_maintenance*.py`** (modify) -- unit-test DR primitives (flags, key/metadata, metric, loud-fail) + hot_merge (merge-only) + env-threshold pass-through (mocked subprocess/boto/connection; real code paths).
13. **`docs/runbooks/ducklake-catalog-operations.md`** -- add the DR section (cadence, dump format, restore-drill pointer, freshness-alarm reading), the SNS wiring, the co-tuning knobs + T2.19 numeric-tuning forward-pointer, and the T2.19-gated ops_compaction decommission runbook.
14. **`docs/ROADMAP-PLATFORM.yaml`** -- T2.18 `progress_note` (FP-B landed; criteria closed = DR + SNS + co-tune mechanism; ops_compaction retirement + final tuning numbers carry to T2.19); leave status open.
15. **`docs/SESSION_LOG.md`** -- session entry.
16. **Execute Verification Plan** -- run each step in order. Loop until pass. The apply (step 8) is human-gated; the SNS subscription confirmation (step 9) is a one-time manual email click; live invoke gates (11-14) run post-deploy. If a V3 step fails unrecoverably, STOP and root-cause (Decision 55) -- do not relax a guardrail or DR flag to pass.
17. Report: what was implemented, the live VP results (DR object + engine tag + metric; hot_merge file deltas; both alarms wired to SNS), and confirmation that ops_compaction is behaviourally untouched and T2.18 remains open pending T2.19.
