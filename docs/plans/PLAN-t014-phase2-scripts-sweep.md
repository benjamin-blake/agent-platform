# Plan

## Intent
Implement T0.14 Phase 2 -- the Windows-assumption sweep across the `scripts/` directory. Replace brittle hardcoded "prefer `.venv/Scripts/python.exe`" venv resolution and OS-misaligned activate hints so fresh Linux containers (Claude Code on the web, Codespaces) run the executor formatters, executor step runner, session preflight, and agent telemetry writer end-to-end without misleading errors. Phase 1 (instruction-layer sweep) shipped in `agents-md-and-instruction-sweep`. Phase 3 (`.agents/`, `src/data/handlers/CLAUDE.md`, `setup.py`) is explicitly deferred to a follow-on plan.

## Plan Type
IMPLEMENTATION

## Verification Tier
V2

## Branch
agent/t014-phase2-scripts-sweep

## Phase
Tier T0 (T0.14 Phase 2). Foundational substrate work that enables Codespaces/CC-on-web reachability (T2.11) without depending on AWS or Lambda.

## Scope
| File | Action | Purpose |
|------|--------|---------|
| `scripts/executor/formatters.py` | Modify | Flip THREE venv-resolution blocks from Windows-first to Linux-first: (a) `_VENV_PYTHON` at lines 19-25, (b) `_venv_ruff` inside `auto_format_test_files()` at lines 77-85, (c) `_venv_ruff` inside `_run_ruff_format()` at lines 248-255 (this third block was missed in the first round of scoping; the two ruff resolvers must be flipped consistently or one function resolves Windows-first while the other Linux-first). Add `# Why: CD.2/CD.3 -- Windows fallback` inline on the same line as each surviving `python.exe`/`ruff.exe` literal so the rationale survives and VP step 5's per-line filter recognises it. |
| `scripts/executor/step_runner.py` | Modify | Flip the venv-Python resolution block (lines 176-182) from Windows-first to Linux-first with the same `# Why: CD.2 / CD.3` annotation. |
| `scripts/session_preflight.py` | Modify | (a) Extract a new helper `_print_activate_hint() -> None` (placed near `check_venv()`) that branches on `sys.platform == "win32"` to print the correct `Run: source .venv/<...>/activate` line. (b) Replace the inline `print("Run: source .venv/Scripts/activate")` at line ~1225 with a call to `_print_activate_hint()`. The helper-extraction is required so `TestActivateHint` can call the helper directly under `capsys` without patching all of `main()`'s Athena/git/SSO dependencies. (c) Clarify the `check_venv()` docstring at line ~58 to enumerate both layouts with inline `CD.2`/`CD.3` tokens (e.g., "`bin/python` on Linux/macOS (CD.2 primary), `Scripts/python.exe` on Windows (CD.3 compute-node)"). |
| `scripts/agent_telemetry_writer.py` | Modify | Replace the docstring example at line 9 (`.venv/Scripts/python.exe scripts/agent_telemetry_writer.py ...`) with `bin/venv-python -m scripts.agent_telemetry_writer ...`. |
| `scripts/llm_utils.py` | Modify | Comment-only. Add one-line `# Intentional: CD.3 -- Windows compute-node path; Linux falls through to else` above the `if sys.platform == "win32":` branch at line 47 so the T0.14 strict-grep readers see this is platform-safe by design, not a regression. |
| `scripts/copilot_wrapper.py` | Modify | Comment-only. Same one-line `# Intentional: CD.3 ...` annotation above the `if sys.platform == "win32":` branch at line 49. |
| `tests/test_executor_formatters.py` | Modify | File already exists (154 lines, 3 test classes). Add a new test class `TestVenvPythonResolution` that uses `importlib.reload(scripts.executor.formatters)` inside a `patch.object(pathlib.Path, "exists", side_effect=...)` context so the module-level `_VENV_PYTHON` / `_PROJECT_PYTHON` re-evaluates under the mocked filesystem state. Two cases: (a) both layouts simulated present -> Linux path wins, (b) only Windows layout present -> Windows path is the fallback. Mirror coverage for the ruff binary resolver. |
| `tests/test_executor_step_runner.py` | Modify | File already exists (1667 lines). Add a parallel `TestVenvPythonResolution` class with the same `importlib.reload(scripts.executor.step_runner)` + `Path.exists` patching approach -- `step_runner._PROJECT_PYTHON` is an independent module-level global and needs its own coverage per `tests/CLAUDE.md` per-file-coverage policy. |
| `tests/test_session_preflight.py` | Modify | Add a new test class `TestActivateHint` with two cases asserting the activate hint printed when `check_venv()` returns False uses `.venv/bin/activate` under `sys.platform != "win32"` and `.venv/Scripts/activate` under `sys.platform == "win32"`. Class name is fixed (not `-k`-pattern) so the VP can target it via `::TestActivateHint` per `tests/CLAUDE.md` discipline. |

## Bundled Recommendations
None. The local rec cache (`logs/.recommendations-log.jsonl`) was not refreshed via `sync_ops pull` in this session because AWS SSO is unavailable from this container. Any rec surfaced during execution must be filed in a follow-on session with SSO available; in particular, **file a follow-on recommendation** to migrate `scripts/executor/formatters.py` and `scripts/executor/step_runner.py` from in-process venv-path resolution to spawning `bin/venv-python` -- deferred per Q1 of the planning session since a larger executor refactor is on the roadmap.

## Acceptance Criteria
- [ ] `scripts/executor/formatters.py`, `scripts/executor/step_runner.py`, and the activate-hint branch in `scripts/session_preflight.py` resolve to the Linux venv path **first** when `sys.platform != "win32"`, with the Windows path retained as fallback.
- [ ] The misleading `Run: source .venv/Scripts/activate` hint in `scripts/session_preflight.py` is replaced with an OS-aware branch.
- [ ] `scripts/agent_telemetry_writer.py` docstring example references `bin/venv-python`, not `.venv/Scripts/python.exe`.
- [ ] No file outside the allowlist (`scripts/executor/formatters.py`, `scripts/executor/step_runner.py`, `scripts/llm_utils.py`, `scripts/copilot_wrapper.py`, `scripts/session_preflight.py`) contains a `python.exe` reference under `scripts/` (verified by VP step 4).
- [ ] `bin/venv-python -m scripts.validate --pre` exits 0.
- [ ] `bin/venv-python -m pytest tests/test_executor_formatters.py tests/test_executor_step_runner.py tests/test_session_preflight.py -q` exits 0, including the new regression cases.
- [ ] `bin/venv-python -c "import scripts.executor.formatters, scripts.executor.step_runner, scripts.session_preflight, scripts.agent_telemetry_writer"` exits 0 on Linux (sanity that the edits did not break import).

## Verification Plan
| # | Phase | Action | Command | Expected Outcome | Fix If |
|---|-------|--------|---------|-------------------|--------|
| 1 | [pre-deploy] | Import sanity on Linux | `bin/venv-python -c "import scripts.executor.formatters as f; import scripts.executor.step_runner as s; import scripts.session_preflight as p; import scripts.agent_telemetry_writer as a; print(f._PROJECT_PYTHON, s._PROJECT_PYTHON)"` | Prints two paths both ending in `.venv/bin/python` (Linux container) | If a path ends in `Scripts/python.exe`, the Linux-first ordering was applied incorrectly. Re-check the conditional branches. |
| 2a | [pre-deploy] | OS-aware activate hint test class exists | `bin/venv-python -c "import ast,sys; t=ast.parse(open('tests/test_session_preflight.py').read()); names={n.name for n in ast.walk(t) if isinstance(n, ast.ClassDef)}; assert 'TestActivateHint' in names, f'TestActivateHint class missing; classes present: {sorted(names)}'; print('TestActivateHint class present')"` | Prints `TestActivateHint class present` | Add the `TestActivateHint` class per Step 8 before re-running. |
| 2b | [pre-deploy] | OS-aware activate hint regression | `bin/venv-python -m pytest tests/test_session_preflight.py::TestActivateHint -q` | Both Linux and Windows cases pass. | If the test fails on Linux, the activate-hint branch in `scripts/session_preflight.py` is mis-conditioned. |
| 3a | [pre-deploy] | Executor venv-resolution test classes exist | `bin/venv-python -c "import ast; needed=[('tests/test_executor_formatters.py','TestVenvPythonResolution'),('tests/test_executor_step_runner.py','TestVenvPythonResolution')]; missing=[(p,c) for p,c in needed if c not in {n.name for n in ast.walk(ast.parse(open(p).read())) if isinstance(n, ast.ClassDef)}]; assert not missing, f'missing test classes: {missing}'; print('venv-resolution test classes present')"` | Prints `venv-resolution test classes present` | Add the `TestVenvPythonResolution` classes per Step 7 before re-running. |
| 3b | [pre-deploy] | Executor formatters + step_runner Linux-first regression | `bin/venv-python -m pytest tests/test_executor_formatters.py::TestVenvPythonResolution tests/test_executor_step_runner.py::TestVenvPythonResolution -q` | Both classes green; module-reload pattern correctly re-evaluates `_VENV_PYTHON` under patched `Path.exists` and Linux candidate wins when both layouts simulated present. | If a test no-ops (passes too quickly), confirm `importlib.reload(module)` is invoked INSIDE the `patch.object(pathlib.Path, "exists", ...)` context manager, not outside. The module-level binding is evaluated at import time and the reload is what re-triggers the patched filesystem check. |
| 4 | [pre-deploy] | `python.exe` allowlist clean across `scripts/` | `bin/venv-python -c "import subprocess; out=subprocess.run(['grep','-rlE','python\\.exe','scripts/','--include=*.py'], capture_output=True, text=True).stdout; actual=set(out.strip().splitlines()) if out.strip() else set(); allow={'scripts/executor/formatters.py','scripts/executor/step_runner.py','scripts/llm_utils.py','scripts/copilot_wrapper.py','scripts/session_preflight.py'}; unexpected=actual-allow; assert not unexpected, f'unexpected python.exe refs: {unexpected}'; print(f'python.exe allowlist clean; files with refs: {sorted(actual)}')"` | Prints `python.exe allowlist clean; files with refs: [...]` with the file list a subset of the allowlist. | If a new file appears in `unexpected`, an unintentional Windows-path reference slipped in -- locate the line via `grep -nE 'python\.exe' <file>` and remove or move into an annotated `# Intentional: CD.3` block. |
| 5 | [pre-deploy] | Every `python.exe` and `ruff.exe` line in the flipped executor files carries an inline `CD.` annotation | `bin/venv-python -c "import sys; files=['scripts/executor/formatters.py','scripts/executor/step_runner.py']; bad=[]; [bad.append((p,lineno,ln.rstrip())) for p in files for lineno,ln in enumerate(open(p).read().splitlines(),1) if (('python.exe' in ln) or ('ruff.exe' in ln)) and ('CD.' not in ln)]; assert not bad, f'unannotated Windows-path lines: {bad}'; print(f'all python.exe/ruff.exe lines in executor files carry CD. annotation')"` | Prints `all python.exe/ruff.exe lines in executor files carry CD. annotation` | Any line returned must be either (a) annotated inline with `# Why: CD.2/CD.3 -- Windows fallback` (or equivalent containing `CD.`), OR (b) removed if it is a stale reference no longer needed after the Linux-first flip. |
| 6 | [pre-deploy] | Full presubmit suite | `bin/venv-python -m scripts.validate --pre` | Exit 0 | Diagnose per the specific failed check. Do not bypass with `--ignore-budget` -- if a check is slow, file a rec, do not skip. |
| 7 | [pre-deploy] | Focused pytest covering all touched test files | `bin/venv-python -m pytest tests/test_executor_formatters.py tests/test_executor_step_runner.py tests/test_session_preflight.py tests/test_executor_postflight.py -q` | All green | Diagnose specifically; preserve existing coverage. |
| 8 | [pre-deploy] | SLOC waiver still in place | `bin/venv-python -c "from pathlib import Path; assert Path('scripts/session_preflight.py').read_text().startswith('# complexity-waiver'); assert Path('scripts/executor/formatters.py').read_text().startswith('# complexity-waiver'); assert Path('scripts/executor/step_runner.py').read_text().startswith('# complexity-waiver'); print('SLOC waiver present')"` | Prints `SLOC waiver present` | All three files already carry the waiver as of branch-base; this confirms our edits did not strip it. If stripped, restore the header line. |

## Constraints
- AGENTS.md Temporary Operational Constraints: STRATEGIC plans suspended -- this work is scoped as a single atomic IMPLEMENTATION plan (9 files, all in scripts/ or tests/).
- AGENTS.md Temporary Operational Constraints (Decision 67 / CD.17): two scope files (`scripts/copilot_wrapper.py`, `scripts/llm_utils.py`) are in `_LAMBDA_SCRIPTS` and ship in `data-pipeline.zip`. The DEFERRED Lambda-deployment marker is in Ordered Step 6a. The Step 5 / Step 6 edits are comment-only with zero runtime behaviour change, so no functional drift between local and deployed image lands in this PR.
- Decision 44 (Executor Self-Modification Boundary): `scripts/executor/formatters.py`, `scripts/executor/step_runner.py`, and `scripts/copilot_wrapper.py` are on the boundary table -- this plan is the required `/plan` -> `/implement` route, never via autonomous executor. Follow-on recs from this work that touch the same files must be filed with `automatable: false`. The mirror test files `tests/test_executor_formatters.py` and `tests/test_executor_step_runner.py` inherit the same routing requirement.
- Decision 60 / Decision 73: `validate --pre` is the edit-loop tier; full CI on PR is the merge gate.
- No emojis in code or commit messages. Plain ASCII hyphens (`-`) instead of em dashes.
- Edits must preserve `# complexity-waiver: decision-43` headers (lines 1 of `scripts/executor/formatters.py`, `scripts/executor/step_runner.py`, `scripts/session_preflight.py`).
- Inline annotations are required: every `python.exe` literal that survives in `scripts/` must either (a) appear on a line carrying `CD.` in a `# Why: ...` or `# Intentional: ...` comment, OR (b) appear inside a clearly Windows-conditional branch whose guard line carries `CD.3` in a comment. This is the discipline VP step 5 enforces.

## Context
- T0.14 in `docs/ROADMAP-PLATFORM.yaml` (lines 1706-1754): "Windows-assumption sweep across the codebase." Phase 1 complete (44 path references swept in `agents-md-and-instruction-sweep`). Remaining: Phase 2 = `scripts/` (this plan), Phase 3 = `.agents/`, `src/data/handlers/CLAUDE.md`, `setup.py` (deferred to a follow-on plan).
- Branch base: `main` at HEAD, post-merge of `agent/config-lambda-split` (T-1.7, PR #363) and `agent/migration-t03-t06-prep` / `agent/t03-pattern-b-agent-auth` (T0.3 prep, PRs #361 #362). No file-overlap risk with currently open feature branches.
- Decisions cited (per decision-scout gate, verdict NO_FLAGS):
  - **Decision 44**: governs route; this plan complies.
  - **CD.2** (pending): Dev surface = Claude Code on the web -- governs the Linux-first ordering.
  - **CD.3** (pending): Windows desktop = compute node only -- governs preservation of Windows-conditional branches in `llm_utils.py` and `copilot_wrapper.py`.
  - **Decision 27**: Git Bash venv Activation Fix via setup.py -- adjacent, not touched (Phase 3 deferral protects).
- The `bin/venv-python` wrapper (T0.1) is the long-term path for venv resolution. Migrating executor formatters/step_runner from in-process resolution to spawning that wrapper is **explicitly deferred** to a planned larger executor refactor; this plan applies only the minimal Linux-first flip.
- SLOC waivers are already in place on all three large files in scope -- no Decision 43 surprise from this work.

## Pre-Implementation Checklist
- [ ] Branch confirmed not on `main` (`git branch --show-current` shows `agent/t014-phase2-scripts-sweep`)
- [ ] docs/PROJECT_CONTEXT.md read
- [ ] DECISIONS.md surveyed (decision-scout returned NO_FLAGS at planning time; re-check Decision 44 boundary)
- [ ] All files in Scope table located and readable
- [ ] Acceptance Criteria understood and verifiable

## Ordered Execution Steps
1. **`scripts/executor/formatters.py`** -- locate the `_VENV_PYTHON` block (lines 20-24), the ruff-binary block inside `auto_format_test_files()` (lines 77-85), AND the second ruff-binary block inside `_run_ruff_format()` (lines 248-255). For each of the THREE blocks, swap the conditional so the Linux candidate (`.venv/bin/python` and `.venv/bin/ruff`) is checked first; the Windows candidate (`.venv/Scripts/python.exe` and `.venv/Scripts/ruff.exe`) becomes the fallback. Place the `# Why: CD.2/CD.3 -- Windows fallback` annotation **inline on the same line as each `python.exe` and `ruff.exe` literal** (not on the preceding line). This is required so VP step 5's inline-annotation check passes -- block-level comments above the literal would not be filterable by per-line greps. Both ruff blocks must be flipped together; flipping only one creates an asymmetric resolver where `auto_format_test_files()` and `_run_ruff_format()` disagree on which binary to prefer.
2. **`scripts/executor/step_runner.py`** -- locate the `_VENV_PYTHON` block (lines 177-181). Apply the same Linux-first flip with the `# Why: CD.2/CD.3 -- Windows fallback` annotation inline on the `python.exe` literal line.
3. **`scripts/session_preflight.py`** -- (a) Extract a new module-level helper `def _print_activate_hint() -> None:` placed adjacent to `check_venv()` (around line 70). The helper body: `if sys.platform == "win32": print("Run: source .venv/Scripts/activate  # CD.3 -- Windows compute-node")  else: print("Run: source .venv/bin/activate")`. (b) Replace the inline `print("Run: source .venv/Scripts/activate")` at line ~1225 with a single call `_print_activate_hint()`. Helper extraction is required by Step 8 below so `TestActivateHint` can call the helper in isolation without patching all of `main()`'s Athena / git / SSO surface. (c) Update the `check_venv()` docstring at line ~58 from "Scripts/python.exe on Windows, bin/python on Linux/macOS" to "`bin/python` on Linux/macOS (CD.2 primary), `Scripts/python.exe` on Windows (CD.3 compute-node)". The inline `CD.2`/`CD.3` tokens are intentional so the rationale is recoverable from the file itself.
4. **`scripts/agent_telemetry_writer.py`** -- at line 9, replace `.venv/Scripts/python.exe scripts/agent_telemetry_writer.py \` with `bin/venv-python -m scripts.agent_telemetry_writer \` in the docstring example. After this edit the file should no longer contain `python.exe` -- VP step 4 verifies it is absent from the allowlist.
5. **`scripts/llm_utils.py`** -- comment-only: replace `if sys.platform == "win32":` at line 47 with `if sys.platform == "win32":  # Intentional: CD.3 -- Windows tasklist branch`. Keep the annotation under 127 chars (ruff E501). VP step 5 does NOT scan this file, so a guard-line comment (rather than a per-literal inline annotation) is sufficient.
6. **`scripts/copilot_wrapper.py`** -- comment-only: same edit as Step 5 applied to `if sys.platform == "win32":` at line 49.
6a. **Lambda deployment posture** -- `scripts/copilot_wrapper.py` and `scripts/llm_utils.py` are listed in `_LAMBDA_SCRIPTS` (`scripts/build_lambda.py:41,44`) and ship inside `data-pipeline.zip`. Per AGENTS.md Temporary Operational Constraints and Decision 67: `DEFERRED: bin/venv-python -m scripts.build_lambda --deploy + bin/venv-python -m scripts.run_scheduled_agent --smoke-test data-pipeline (pending Decision 67 reversal)`. The edits in Steps 5 and 6 are comment-only with zero runtime behaviour change, so no Lambda-runtime drift will land in this PR; deployment remains parked under Decision 67 until CD.17 reverses and a follow-on plan exercises the deploy path.
7. **`tests/test_executor_formatters.py`** and **`tests/test_executor_step_runner.py`** -- in EACH file, add a new test class `TestVenvPythonResolution` containing at minimum two test methods plus a concrete teardown fixture.

   **Teardown fixture (autouse, class-scoped):**
   ```python
   @pytest.fixture(autouse=True)
   def _restore_module_state(self):
       """Reload the executor module after each test so the patched _PROJECT_PYTHON does not leak into other test classes that import it."""
       yield
       import importlib
       import scripts.executor.formatters as _mod  # or step_runner in the parallel file
       importlib.reload(_mod)
   ```

   **Test methods:**
   - `test_linux_layout_preferred_when_both_present`: use `patch.object(pathlib.Path, "exists", lambda self: True)` (or a per-path `side_effect` returning True for both `bin/python` and `Scripts/python.exe`) so both venv layouts appear present. INSIDE the `with patch(...)` context, call `importlib.reload(scripts.executor.formatters)` (resp. `scripts.executor.step_runner`). Assert `scripts.executor.formatters._PROJECT_PYTHON.endswith("bin/python")` (resp. step_runner). The reload is critical: the module-level `_VENV_PYTHON` is bound at first import using the real `Path.exists`, so without reload the test no-ops.
   - `test_windows_fallback_when_linux_missing`: `side_effect=lambda self: "python.exe" in str(self) or "ruff.exe" in str(self)` (True only for Windows paths, False for `bin/python`). Reload inside patch. Assert `"python.exe" in scripts.executor.formatters._PROJECT_PYTHON` confirming the Windows fallback path is preserved.

   For `formatters.py` ONLY, add a third method `test_ruff_resolution_linux_first` that exercises the ruff-binary resolution. Since the ruff binary is resolved inline inside `auto_format_test_files()` and `_run_ruff_format()` (not module-level), this test patches `Path.exists`, calls the function on a fixture file, captures the `ruff_cmd_prefix` (e.g., via a `subprocess.Popen` mock and inspection of `call_args`), and asserts the resolved prefix ends in `bin/ruff` not `Scripts/ruff.exe` when both candidates are simulated present.

   The autouse `_restore_module_state` fixture ensures subsequent tests in the same pytest session see the real `_PROJECT_PYTHON`, not the patched value. Without this teardown the next test class that touches these modules will see stale patched state and fail unpredictably.
8. **`tests/test_session_preflight.py`** -- add a new test class `TestActivateHint` with two methods: `test_activate_hint_linux` (patch `sys.platform` to `"linux"` via `monkeypatch.setattr(sys, "platform", "linux")`, call `session_preflight._print_activate_hint()` directly, use `capsys.readouterr().out` to assert `.venv/bin/activate` appears AND `.venv/Scripts/activate` does not) and `test_activate_hint_windows` (mirror with `sys.platform = "win32"` and the opposite assertion). Calling the helper directly is what the Step 3 extraction enables; do NOT attempt to drive `main()` end-to-end -- it requires Athena, git, SSO, and the existing `_disable_athena_queries` autouse fixture, which is out of scope for this regression test. Class name `TestActivateHint` is fixed so VP step 2b targets it via `::TestActivateHint`.
9. **Execute Verification Plan** -- run each step in order. Loop until pass. If any V2 check fails unrecoverably, stop and analyse the root cause per Decision 55; do not introduce rescue agents or workaround loops.
10. **Commit and push** -- `git add` the scope files, commit with message `feat(t014-phase2): scripts/ Windows-assumption sweep (T0.14 Phase 2)`, push to `agent/t014-phase2-scripts-sweep` with `-u origin`.
11. **Report** -- summarise: which files were edited, what greps now return, validate/pytest exit codes, any follow-on recs surfaced (in particular the `bin/venv-python` migration rec for the executor refactor).
