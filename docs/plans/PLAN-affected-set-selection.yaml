schema_version: 1
slug: "affected-set-selection"
intent: >-
  Upgrade the fast --pre presubmit tier from an edited-set (test files literally in the diff) to a
  live, per-run affected-set (tests affected by the diff) so a source-only PR and a test broken by a
  change it does not itself contain are caught pre-merge -- hardening the merge path every
  self-improvement change flows through (co-resolves rec-2638).
plan_type: IMPLEMENTATION
verification_tier: V2
plan_path: docs/plans/PLAN-affected-set-selection.yaml
phase: >-
  Platform CI / fast-tier presubmit hardening. AMENDS Decision 73's fast-tier selection mechanism
  (its 2nd amendment, after the PR #334 explicit-paths fix). Not tied to a single tier_item;
  co-resolves rec-2638. Leaves KG.13 (deferred Bazel-style Test-Impact-Analysis + selection/coverage
  caches, gated by T3.11 c5's revisit trigger) undisturbed and still gated; defers the manifest's
  DuckLake table + named-verb registration to T2.36.
scope:
  - file: scripts/checks/deps/affected_tests.py
    action: Create
    purpose: >-
      Live per-run affected-set derivation. Unions FOUR channels over the edited-set, strictly
      additively (selection can only GROW): (1) import-closure reverse-deps via
      scripts/dependency_graph.py; (2) data-edge basename grep (generalizes+retires
      select_roadmap_guard_tests); (3) map_source_to_test() mirror map; (4) conftest-subtree rule.
      Composition + ~35-module cap + loud defer of the transitive residue + exception->edited-set
      fallback + selection-manifest.json emission. Decompose into scripts/checks/deps/ submodules if
      it would cross 500 SLOC (Decision 128 -- never raise the budget).
  - file: scripts/checks/_common.py
    action: Modify
    purpose: >-
      Add a NEW status-aware diff function (git diff --name-status vs merge-base + git ls-files
      --others --exclude-standard for untracked; deleted paths flow through with D status). This is
      the SOLE home of get_changed_files / shared primitives (Decision 104) -- keep qualified-name
      interception (_common.run/ROOT). Keep get_changed_files()'s existing contract (deleted-path
      filtering) unchanged for its existing callers. Co-resolves rec-2638 (untracked new files
      invisible to --pre selection).
  - file: scripts/dependency_graph.py
    action: Modify
    purpose: >-
      Two soundness patches: (i) parse __init__.py as package nodes so facade re-exports (Decision
      124) don't drop edges; (ii) union string-constant module edges from patch("scripts.x.y")
      targets (AST pass over ast.Constant matching ^(src|scripts)(\.\w+)+). Reverse transitive
      closure for the affected-set is computed via nx.ancestors in affected_tests.py. Keep the stable
      public API (build_graph / roots / reverse_deps / forward_closure / ...).
  - file: scripts/validate.py
    action: Modify
    purpose: >-
      Replace the inline edited-set changed_tests computation in main()'s --pre block with a call to
      the new derivation; retire select_roadmap_guard_tests (function + call site) into the general
      data-edge channel; wire the manifest emit/print. main() is the thin CLI surface (Decision 104);
      the new check registers via the registry, not by hand-wiring here.
  - file: scripts/checks/registry.py
    action: Modify
    purpose: >-
      pre_sequence() wiring for any new --pre step introduced by the derivation. WS1 owns this file;
      any registration edit moves in the SAME commit as the tests/test_checks_registry.py floor
      update (frozen-baseline canary coupling).
  - file: scripts/checks/_scaffolding.py
    action: Modify
    purpose: >-
      Batch the currently-serial per-file collect-only pass in
      partition_changed_tests_by_collectability into ONE pytest invocation (~30x saving) to net-fund
      the cap+defer + derivation cost inside the 5-min budget. Preserve the existing loud defer idiom
      (_print_deferred_warning).
  - file: .github/workflows/ci.yml
    action: Modify
    purpose: >-
      Add an actions/upload-artifact step (if: always()) to the pr-validate job to capture
      selection-manifest.json as the PR-context observability capture. Must keep
      validate_ci_workflow_guards green (jobs-and-flags / signal-green needs / fetch-depth /
      concurrency invariants). No AWS creds are added to pr-validate.
  - file: tests/checks/deps/test_affected_tests.py
    action: Create
    purpose: >-
      Incident-replay + per-channel + additive-invariant + cap/defer + manifest + graceful-degrade
      regression tests that exercise the REAL selector (not mocks of it) and fail when the relevant
      channel is disabled. New package dir carries __init__.py; self-contained (no cross-test
      imports), conftest layered per Decision 131.
  - file: tests/test_dependency_graph.py
    action: Modify
    purpose: Tests for the two soundness patches (facade __init__ package nodes; patch-string module edges).
  - file: tests/test_checks_registry.py
    action: Modify
    purpose: >-
      Update REQUIRED_PRE_CHECKS floor if a new --pre check is registered (same-commit canary
      coupling). This file is also _common.py's coverage-checker mapped home.
  - file: tests/validate/test_changed_files.py
    action: Modify
    purpose: >-
      Tests for the new status-aware diff function (deletions flow through with D; untracked new
      files visible -- rec-2638) while the existing get_changed_files() deleted-path-dropping tests
      stay green (contract preserved).
  - file: tests/validate/test_pytest_diff.py
    action: Modify
    purpose: Test that the collect-only partition runs as a single batched pytest invocation.
  - file: tests/validate/test_tiers.py
    action: Modify
    purpose: >-
      Remove/adapt the retired select_roadmap_guard_tests tests; add a subsumption proof that a
      roadmap-YAML change still selects the roadmap-guard tests via the general data-edge channel.
bundled_recommendations:
  - rec-2638
infrastructure_dependencies: []
acceptance_criteria:
  - >-
    A source-only diff (no test file edited) that changes a module selects that module's
    reverse-dependency test modules in --pre via the import-closure channel, proven by exercising the
    real selector (not a mock of it).
  - >-
    Incident A: a YAML entry-count change selects the test that READS that YAML (data-edge channel);
    the selection drops it when the data-edge channel is disabled (channel has teeth).
  - >-
    Incident B: a deleted test file selects the meta-test that READS its bytes (data-edge channel
    over deleted paths, made visible by the status-aware diff's D-status entries).
  - >-
    A facade-only (__init__.py re-export, Decision 124) import selects its dependents (dependency_graph
    __init__ package-node soundness patch).
  - >-
    A patch("scripts.x.y")-string-only dependency selects the target module's tests (dependency_graph
    string-constant module-edge soundness patch).
  - >-
    Additive-only invariant: for any diff, affected-selection is a superset of the edited-set,
    including under cap/overflow -- an edited-set member (and direct reverse-deps + data-edge hits) is
    never deferred; only the transitive residue is deferred.
  - >-
    Untracked new files are visible to the status-aware diff / --pre selection (rec-2638
    co-resolution), while get_changed_files()'s existing deleted-path-dropping contract is unchanged
    for its existing callers.
  - >-
    select_roadmap_guard_tests is removed (function + call site) and its behavior is subsumed by the
    general data-edge channel: a roadmap-YAML change still selects the roadmap-guard tests.
  - >-
    A per-run selection-manifest.json is emitted (sha, status-aware diff, per-test provenance/channel,
    selected/capped/deferred, timings) to a gitignored path, printed in the CI log, and uploaded as a
    GitHub Actions artifact; the aws s3 cp path lazy-imports boto3 and degrades gracefully (loud skip)
    when creds/boto3 are absent (the no-creds fast tier); the manifest is NEVER a selection input.
  - >-
    The collect-only partition in partition_changed_tests_by_collectability runs as a single batched
    pytest invocation.
  - >-
    bin/venv-python -m scripts.validate --pre passes green within the 5-minute fast-tier budget; any
    new --pre check registration + the tests/test_checks_registry.py floor update + any docs move in
    the SAME commit; every touched source file stays <= 500 SLOC (decompose, never raise -- Decision
    128 / 130).
verification_plan:
  - step: 1
    phase: pre-deploy
    action: >-
      Import-closure channel: a source-only diff changing a module selects that module's
      reverse-dependency test modules via the real selector.
    command: bin/venv-python -m pytest tests/checks/deps/test_affected_tests.py::TestImportClosureChannel -v
    expected: Test passes; a source-only change selects the reverse-dep test module (no test file was edited).
    fix_if: >-
      The import-closure channel does not walk nx.ancestors from the changed module to its test
      modules, or the source-only case selects nothing.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: >-
      Exercised by TestImportClosureChannel as a standing pytest regression in the full post-merge
      tier; graduating a per-scenario selector into the verification-checks kernel duplicates the
      suite-level guard without adding independent signal.
  - step: 2
    phase: pre-deploy
    action: >-
      Incident A (data-edge): a YAML entry-count change selects the test that reads that YAML, and
      DISABLING the data-edge channel drops it (channel has teeth).
    command: bin/venv-python -m pytest tests/checks/deps/test_affected_tests.py::TestDataEdgeChannel -v
    expected: Both the positive (selected) and channel-disabled (dropped) assertions pass.
    fix_if: The data-edge basename alternation does not match the YAML basename inside the reading test, or the disabled-channel case still selects it.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: >-
      Standing pytest regression (TestDataEdgeChannel) in the full tier; the data-edge property is a
      pure-function grep over a constructed fixture repo with no live-state seam a kernel primitive
      would capture better than the test.
  - step: 3
    phase: pre-deploy
    action: >-
      Incident B (deleted-path data-edge): a deleted test file selects the meta-test that reads its
      bytes, via the status-aware diff's D-status entries.
    command: bin/venv-python -m pytest tests/checks/deps/test_affected_tests.py::TestDeletedPathDataEdge -v
    expected: Test passes; the deleted path (D status) still flows through and its basename selects the meta-test that reads it.
    fix_if: The status-aware diff drops deleted paths, or the data-edge channel is not fed the deleted basenames.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: Standing pytest regression (TestDeletedPathDataEdge) in the full tier; duplicating it as a kernel check adds no independent signal.
  - step: 4
    phase: pre-deploy
    action: Facade __init__ soundness patch -- a facade-only (__init__.py re-export) import selects its dependents.
    command: bin/venv-python -m pytest tests/checks/deps/test_affected_tests.py::TestFacadeNodeSoundness -v
    expected: Test passes; edges through the package (__init__) node are not dropped, so dependents of a facade re-export are selected.
    fix_if: dependency_graph.build_graph still skips __init__.py (no package node), so facade re-export edges are missing.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: Standing pytest regression (TestFacadeNodeSoundness); the graph-soundness property is covered by the suite, not better captured by a kernel primitive.
  - step: 5
    phase: pre-deploy
    action: patch-string soundness patch -- a patch("scripts.x.y")-string-only dependency selects the target module's tests.
    command: bin/venv-python -m pytest tests/checks/deps/test_affected_tests.py::TestPatchStringEdgeSoundness -v
    expected: Test passes; the string-constant module edge is unioned into the graph and the target's tests are selected.
    fix_if: The AST pass over ast.Constant does not match ^(src|scripts)(\.\w+)+ patch targets, so the string edge is missing.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: Standing pytest regression (TestPatchStringEdgeSoundness); duplicating in the kernel adds no independent signal.
  - step: 6
    phase: pre-deploy
    action: >-
      Additive-only invariant -- affected-selection is a superset of the edited-set for any diff,
      including under cap/overflow (edited-set + direct reverse-deps + data-edge hits are never
      deferred).
    command: bin/venv-python -m pytest tests/checks/deps/test_affected_tests.py::TestAdditiveOnlyInvariant tests/checks/deps/test_affected_tests.py::TestCapAndDefer -v
    expected: Both classes pass; selection always contains the edited-set, and overflow defers only the transitive residue.
    fix_if: A cap/overflow path defers an edited-set member (or a direct reverse-dep / data-edge hit), violating the additive-only invariant.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: >-
      The additive-only invariant is exercised by TestAdditiveOnlyInvariant/TestCapAndDefer as a
      standing pytest regression in the full tier; it is a pure-function property over the selector
      with no live-state or wall-clock seam, so a verification-checks kernel test_selector row would
      re-run the same pytest without independent signal.
  - step: 7
    phase: pre-deploy
    action: >-
      rec-2638 co-resolution -- untracked new files are visible to the status-aware diff; the
      existing get_changed_files() deleted-path-dropping contract is unchanged.
    command: bin/venv-python -m pytest tests/validate/test_changed_files.py -v
    expected: >-
      All tests pass: the new status-aware diff surfaces untracked (git ls-files --others) and
      deleted (D-status) paths; the pre-existing get_changed_files() tests (drops deleted paths) stay
      green.
    fix_if: The status-aware diff omits untracked paths, or the change regressed get_changed_files()'s existing contract.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: Standing pytest regression (test_changed_files.py) in the full tier; the diff-manifest behavior is suite-covered.
  - step: 8
    phase: pre-deploy
    action: select_roadmap_guard_tests retirement -- the special-case function is removed from scripts/.
    command: bash -c '! grep -rn "def select_roadmap_guard_tests" scripts/'
    expected: grep finds no definition of select_roadmap_guard_tests anywhere under scripts/ (exit 0).
    fix_if: The special-case function still exists; it was not generalized into the data-edge channel and removed.
    hermetic: false
    graduation: graduate
    graduation_check_id: "select-roadmap-guard-tests-retired"
  - step: 9
    phase: pre-deploy
    action: Subsumption proof -- a roadmap-YAML change still selects the roadmap-guard tests via the general data-edge channel.
    command: bin/venv-python -m pytest tests/validate/test_tiers.py::TestRoadmapGuardSubsumption -v
    expected: Test passes; changing a roadmap YAML selects the tests that reference its basename, via the general channel (not the retired special case).
    fix_if: The data-edge channel does not reproduce the roadmap-guard selection the retired function provided.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: Standing pytest regression (TestRoadmapGuardSubsumption); the subsumption equivalence is suite-covered, not a kernel primitive.
  - step: 10
    phase: pre-deploy
    action: Batched collect-only -- partition_changed_tests_by_collectability runs one pytest invocation, not one per file.
    command: bin/venv-python -m pytest tests/validate/test_pytest_diff.py -v
    expected: Tests pass, including the assertion that collect-only is invoked exactly once for a multi-file batch.
    fix_if: The partition still spawns one collect-only subprocess per file (serial), not a single batched invocation.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: Standing pytest regression (test_pytest_diff.py); the batching behavior is suite-covered.
  - step: 11
    phase: pre-deploy
    action: >-
      Registry canary coupling -- the growth-safe floor + scaffold-anchor order stay green after any
      pre_sequence wiring (same-commit baseline update).
    command: bin/venv-python -m pytest tests/test_checks_registry.py -v
    expected: All tests pass; REQUIRED_PRE_CHECKS floor and FROZEN_PRE_SCAFFOLDS order hold after the wiring + baseline update land together.
    fix_if: A new --pre step was registered without the same-commit floor update, or a scaffold-anchor was perturbed.
    hermetic: false
    graduation: not-applicable
  - step: 12
    phase: pre-deploy
    action: >-
      End-to-end + net-budget proof -- the full fast tier runs green within the 5-min budget with live
      affected-set selection active (batched collect-only net-funds the added derivation cost).
    command: bin/venv-python -m scripts.validate --pre
    expected: "Validation Summary (scope: pre): All checks passed; elapsed under the 5-minute fast-tier budget (no budget-breach rec filed)."
    fix_if: The derivation errors (should fall back to edited-set loudly, never silently shrink), reds a check, or blows the budget (the batched collect-only saving did not net-fund the derivation).
    hermetic: false
    graduation: not-applicable
constraints:
  - No SLOC budget raises -- decompose past-budget files into a scripts/checks/deps/ facade package (Decision 128 / 80 / 104 / 124); never raise config/sloc_budgets.yaml. New + modified tests/ files are whole-repo scanned (Decision 130).
  - Never add a check to .github/workflows/ci.yml without adding it to validate.py / the checks registry first (validate.py is the single source of truth). The ci.yml edit here is an artifact-upload STEP, not a check; it must keep validate_ci_workflow_guards green.
  - No direct writes to logs/*.jsonl -- ops portal only (Single Portal Invariant, Decision 84).
  - No terraform apply; no .tf files in scope; no edits on main.
  - The selection-manifest.json is an OUTPUT/observability artifact ONLY -- NEVER a selection input. The derivation always recomputes live (no persisted selection cache, no coverage cache), preserving the no-drift property.
  - Selection is STRICTLY ADDITIVE over the edited-set -- it can only GROW, never shrink. On derivation exception, fall back to edited-set and print loudly; never silently shrink (Decision 55).
  - S3 upload stays best-effort/async so it never counts against the 5-min budget assertion (Decision 73); it degrades to a loud skip (lazy boto3) in the no-creds fast tier -- never silent (Decision 55).
  - No rescue agents or workaround loops (Decision 55); on an unrecoverable failure, stop and RCA.
  - "WS1 owns scripts/checks/registry.py (pre_sequence wiring) AND the tests/test_checks_registry.py frozen-baseline tests -- any new --pre registration + the baseline update + any docs MUST land in the SAME commit (canary trap rec-2673/2674/2675)."
  - Do NOT add a DuckLake table or a warehouse named-verb for the manifest -- that registration is deferred to T2.36. Do NOT file a KG.13 test-impact/caching tier_item and do NOT arm the Decision 80 Bazel/Pants revisit trigger.
  - Every pre-deploy VP step carries a graduation disposition; graduate steps must produce a matching verification-checks registry row at implement time or validate_graduation_completeness reds the feat PR (Decision 132).
  - "Scope-drift check at implement start -- confirm the working tree matches this Scope table before editing; out-of-scope bugs become recommendations via scripts.ops_data_portal, not inline fixes."
context:
  - "AMENDS Decision 73's fast-tier selection mechanism -- its 2nd amendment, after the PR #334 explicit-paths fix; discharges D73's Known Gap ('pytest --picked may be upgraded if false-negatives accumulate'). Builds on Decision 80 (import-graph oracle, sanctioned tool-free affected-set path), 104 (checks registry / _common sole-home), 124 (facade re-export pattern), 131 (mirror map / conftest hierarchy / __init__ per package), 84 (warehouse SoT + numbering), 86 (agent-first; rationale->Decision), 128/130 (SLOC decompose-by-default, whole-repo incl tests), 55 (fail-loud), 132 (graduation enforced), 48 (verification tier)."
  - "KG.13 BOUNDARY (load-bearing; decision-scout D80 NOTE, no pivot needed): KG.13 is the deferred Bazel-style Test-Impact-Analysis + selection/coverage-cache work, gated by T3.11 c5's revisit trigger (concurrency>1 AND (KG.13 filed OR fast-tier-budget breach)). This design is a LIVE, cacheless, strictly-additive, tool-free (ast/networkx) derivation -- NOT a selection cache and NOT a coverage cache -- so it lands in Decision 80 pt3's sanctioned bucket, not pt4's deferred orchestrator bucket. The new Decision must state explicitly that it does NOT file a KG.13 tier_item and does NOT arm the revisit trigger, so a later reader cannot mistake it for KG.13's realization. KG.13 stays deferred and undisturbed."
  - "DECISION-SCOUT D73-budget NOTE (net-budget math): the added cost (import-graph build ~2.2s + basename grep + manifest emit) is net-funded by batching the currently-serial per-file collect-only pass into one pytest invocation (~30x saving); the best-effort/async S3 cp never counts against the budget assertion. VP step 12 runs the real `--pre` and asserts green-under-budget (no budget-breach rec). p90 target ~2-2.5 min."
  - "DECISION-SCOUT D48-tier NOTE (V2 is correct): the .github/workflows/ci.yml edit is an artifact-upload step (not in D48's exhaustive V3 trigger list) and the manifest S3 object is best-effort observability, not a contract another service consumes (pr-validate has no creds; it degrades). V2 is the correct classification (avoids the rec-448 under-classification failure D48 was born from); the substantive verification is unit-level incident replay against the REAL selector."
  - "CROSS-DEPENDENCY with parallel WS3 (aws-test-hermeticity) plan being written concurrently: WS1 (this plan) OWNS scripts/checks/registry.py pre_sequence wiring AND the FROZEN_PRE_SEQUENCE baseline tests (tests/test_checks_registry.py). WS3 owns tests/conftest.py and DEFERS its own check registration to a post-WS1 rebase. WS1 lands first; WS3 rebases -- do not let WS3's conftest changes and WS1's registry/baseline changes collide."
  - "Manifest path is gitignored (e.g. logs/debug/selection-manifest.json -- confirmed git check-ignore); never committed. Printed in the CI log + uploaded as a GitHub Actions artifact in pr-validate. The aws s3 cp to the data-lake ci/selection/ prefix is best-effort and creds-gated: pr-validate has permissions contents:read only (no OIDC, no AWS creds) and requirements-fast.txt omits boto3, so the upload lazy-imports boto3 and degrades to a loud skip (mirrors ensure_fresh_dq_results / _file_budget_breach_rec CI-guard patterns). The Actions artifact is the PR-context capture; S3 durable store + DuckLake query surface deferred to T2.36."
  - "get_changed_files() (scripts/checks/_common.py ~line 42) filters out deleted paths (and untracked never appear -- git diff only); the NEW status-aware diff function is added ALONGSIDE it and get_changed_files()'s contract is preserved for existing callers (tests/validate/test_changed_files.py asserts the deleted-path drop). Untracked visibility primarily fixes LOCAL --pre under-checking (rec-2638); CI checkouts contain only committed files."
  - "dependency_graph.reverse_deps returns DIRECT predecessors only; the affected-set needs the full reverse transitive closure -- compute via nx.ancestors in affected_tests.py. build_graph currently skips __init__.py (soundness patch (i) adds package nodes) and does not union patch-string module edges (soundness patch (ii))."
  - "map_source_to_test() (scripts/test_coverage_checker.py) is USED read-only (channel 3); not modified. It returns a test file Path, a test PACKAGE DIRECTORY (concern-split), or None (scripts/executor/**, scripts/ops_portal/** per Decision 124)."
  - "Decision draft (slug 'Decision affected-set-selection'): the number is assigned by the implement subagent at final rebase (main moves ~13/day; numbers collide -- Decision 77 precedent) and lands IN the feat PR via the ops portal (Decision 84 numbering authority), not this plan PR. Draft ratifies: (a) the four-channel live derivation + cap+defer + soundness patches (facade nodes, string edges, status-aware diff incl. deletions/untracked); (b) selection manifests as append-only S3 observability artifacts consumed ONLY by CI-RCA escape-attribution, explicitly NOT a selection input; (c) DuckLake/named-verb registration deferred to T2.36 with trigger 'first written cross-run query'. It AMENDS Decision 73 (2nd amendment), builds on 80/104/124/131, and cross-references KG.13 as deliberately undisturbed (files no KG.13 tier_item, arms no revisit trigger)."
  - "Bundled rec-2638 (open, manual, scripts/checks/_common.py): 'validate --pre under-checks UNTRACKED new files' -- co-resolved by the status-aware diff's git ls-files --others --exclude-standard leg. Include Resolves: rec-2638 in the feat squash-merge commit body."
  - "Decision-scout verdict + CITE list (verbatim decision ids): FLAGS_FOUND (3 NOTE flags, no BLOCK, all resolved without pivot). CITE: 73, 104, 80, 131, 124, 84, 55, 86, 128, 130, 132, 48. RELATED (lineage, non-governing): 60, 43, 129. KG.13 (roadmap gap in ROADMAP-PLATFORM.yaml T3.11 c5) cross-referenced as deliberately undisturbed."
  - "gates: decision-scout=FLAGS_FOUND (3 NOTE flags, no BLOCK, all resolved in-plan without pivot); plan-critique=DEFERRED_TO_OVERSEER (nested-gate completion notification did not reach the planning agent -- overseer runs plan-critique as a sibling subagent before merging this plan PR or spawning /implement)."
pre_implementation_checklist:
  - Branch confirmed not on main (feat branch off latest origin/main).
  - docs/PROJECT_CONTEXT.md read.
  - DECISIONS.md read (73, 80, 104, 124, 128, 130, 131, 55, 84, 86, 132, 48; KG.13 boundary in ROADMAP-PLATFORM.yaml T3.11 c5).
  - All files in scope located and readable; scope-drift check run at implement start.
  - Acceptance criteria understood and verifiable; incident-replay tests exercise the REAL selector (not mocks of it).
  - "Coordinated with parallel WS3 (aws-test-hermeticity) -- WS1 lands registry.py + baseline first; WS3 rebases its check registration after."
execution_steps:
  - "scripts/checks/_common.py -- add the status-aware diff function (git diff --name-status vs merge-base + git ls-files --others --exclude-standard; deleted paths carry D status; untracked surfaced). Keep get_changed_files()'s deleted-path-dropping contract unchanged; qualified-name interception preserved."
  - "scripts/dependency_graph.py -- soundness patch (i): add __init__.py package nodes so facade re-export edges are retained; soundness patch (ii): union string-constant module edges from patch(\"scripts.x.y\") targets via an AST pass over ast.Constant matching ^(src|scripts)(\\.\\w+)+. Keep the stable public API."
  - "scripts/checks/deps/affected_tests.py -- implement the four-channel derivation (import-closure reverse-transitive via nx.ancestors; data-edge basename alternation over changed+deleted basenames; map_source_to_test mirror map; conftest-subtree rule), union with the edited-set, ~35-module cap keeping edited-set + direct reverse-deps + data-edge hits and deferring only the transitive residue via the existing _print_deferred_warning idiom, exception->edited-set loud fallback, and selection-manifest.json emission (sha, status-aware diff, per-test provenance/channel, selected/capped/deferred, timings). Keep <500 SLOC or decompose into scripts/checks/deps/ submodules (Decision 128)."
  - "scripts/checks/_scaffolding.py -- batch the serial per-file collect-only pass in partition_changed_tests_by_collectability into ONE pytest invocation; preserve the loud defer idiom."
  - "scripts/validate.py -- replace the inline edited-set changed_tests computation in main()'s --pre block with the new derivation; retire select_roadmap_guard_tests (function + call site); print the manifest."
  - "scripts/checks/registry.py + tests/test_checks_registry.py -- if a new --pre step is registered, wire pre_sequence() AND update REQUIRED_PRE_CHECKS in the SAME commit (canary coupling); keep FROZEN_PRE_SCAFFOLDS order intact."
  - ".github/workflows/ci.yml -- add an actions/upload-artifact (if: always()) step to pr-validate capturing selection-manifest.json; keep validate_ci_workflow_guards green; add NO AWS creds to pr-validate."
  - "Tests -- create tests/checks/deps/test_affected_tests.py (incident A/B, facade, patch-string, additive-invariant, cap/defer, manifest, graceful-degrade, exception-fallback, all against the real selector); extend tests/test_dependency_graph.py (both soundness patches); tests/validate/test_changed_files.py (status-aware diff: deletions + untracked, rec-2638); tests/validate/test_pytest_diff.py (batched collect-only); tests/validate/test_tiers.py (retire guard-test tests + subsumption proof)."
  - "Draft the Decision (slug 'Decision affected-set-selection') VERBATIM; the implement subagent assigns the next free number at final rebase and lands it in the feat PR via the ops portal."
  - "Execute Verification Plan -- run each step; loop until pass; on an unrecoverable failure stop and RCA (Decision 55)."
  - "Report: what was implemented, verification results, the assigned Decision number, and rec-2638 closure (Resolves: rec-2638 in the squash-merge body)."
work_areas: []
rollback: >-
  Pure additive/selection change with no infra or data migration. Revert the feat PR to restore the
  edited-set selection; the manifest is observability-only so no downstream consumer breaks. rec-2638
  reopens if reverted.
