# Plan

## Intent
Restore CI health on `main` so the merge gate, the L8 drift canary, the sandbox auto-apply pipeline, and the CI-RCA diagnostic loop all work -- the operational substrate every future platform/product increment depends on (Decision 73 forward-fix model).

## Plan Type
IMPLEMENTATION

## Verification Tier
V3

## Plan Path
docs/plans/PLAN-ci-health-restoration.md

## Phase
Platform infrastructure -- CI health maintenance (forward-fix, Decision 73). Adjacent to platform roadmap T2.15 (CI verification-coverage restoration) and T1.13 (CI-RCA methodology contract); this plan is the tactical green-main precursor and deliberately does NOT pre-empt either (see Context).

## Scope
| File | Action | Purpose |
|------|--------|---------|
| `tests/test_session_postflight.py` | Modify | #1a: add `check_sso` mock (+ hermetic drain/sync mocks) to `test_auto_recommendation_sync_in_output` so it exercises the credentials-available branch deterministically. This is the ONLY fix that turns `main` + Canary green. |
| `scripts/verifiers/schema_integrity.py` | Modify | #1b: treat "Table not found in Athena" as SKIP (not WARN-drift) so unprovisioned telemetry tables stop emitting noise; self-heals when tables are created. |
| `tests/test_verifiers/test_schema_integrity.py` | Modify | #1b: cover the skip-on-missing-table behaviour (100% new-code coverage, tests/CLAUDE.md). |
| `.github/workflows/terraform-apply-sandbox.yml` | Modify | #2: inject the 4 required `TF_VAR_*` inputs; add a fail-closed pre-plan assert; document the 2 new secrets as a collocated comment. |
| `.github/workflows/ci-rca.yml` | Modify | #3: pre-fetch failed-run logs in a workflow step + scope `--allowedTools` so the agent can diagnose and `file_rec`; remove `AWS_PROFILE=agent_platform` (defensive). #4: add a GitHub-native dedup guard keyed by `{workflow}+{head_sha}` + a `force_rca` dispatch input. |
| `.claude/agents/scheduled/ci-rca.md` | Modify | #3: minimal Step-1 edit -- read the pre-fetched log file instead of `gh run view --log-failed`. Bounded edit; does NOT introduce T1.13's typed schema/evidence bundle. |

## Bundled Recommendations
- **rec-657** [code-review, open]: "test_session_postflight.py: run_auto tests lack mock for check_sso and do not assert drain_pending" -- exactly #1a. After #1a lands, update it to resolved via `bin/venv-python -m scripts.ops_data_portal update_rec`.
- Adjacent-but-not-bundled (left open): rec-258 (re-enable CI/pre-commit workflows), rec-1971 (improve `_check_sso` import-failure logging). Out of this plan's blast radius.

## Infrastructure Dependencies
| Item | Detail | Timing |
|------|--------|--------|
| `terraform/personal` root module vars | The plan supplies `account_id` (from `vars.AWS_ACCOUNT_ID`), `owner_email` (from `vars.OWNER_EMAIL`), `platform_dev_external_id` + `platform_admin_external_id` (from new secrets). All four MUST be present before `terraform plan`; a fail-closed assert reds the run if any is empty (never bypasses the guard -- Decision 77). | Pre-plan (workflow) |
| **Human prerequisite (manual, out-of-repo)** | Before #2 can pass, the human adds repo **secrets** `TF_VAR_PLATFORM_DEV_EXTERNAL_ID` and `TF_VAR_PLATFORM_ADMIN_EXTERNAL_ID` (values from the gitignored `terraform/personal/terraform.personal.tfvars`) and **variable** `OWNER_EMAIL`. Confirm `AWS_ACCOUNT_ID` variable already exists (it does -- OIDC role ARNs resolve to `707578707169`). | Before #2 verification |
| Deterministic guard + subagent review | `scripts/terraform_apply_guard.py` (fail-closed) then the `CLAUDE_CODE_OAUTH_TOKEN` subagent review gate the apply. This plan does NOT alter either; var injection sits before `terraform plan`, no re-plan is introduced between guard and apply (no TOCTOU). | Unchanged |
| Apply trigger | The apply workflow fires on push to `terraform/personal/**` or `workflow_dispatch`. This plan edits only the workflow file, so merging it does NOT auto-apply; verification is via `workflow_dispatch` (a real sandbox apply behind the guard -- confirmed desired by the human). | Post-merge |

## Acceptance Criteria
- [ ] #1a: `test_auto_recommendation_sync_in_output` passes when no `agent_platform` AWS profile exists (the runner condition), proven by forcing that environment locally.
- [ ] #1a: `python -m scripts.validate`'s "Unit tests + coverage" check passes; it is the only check that was failing, so `main` push-CI (`main-validate`) and `Main Canary` both go green.
- [ ] #1b: `SchemaIntegrityVerifier` returns `PASS` when the 7 telemetry tables are absent but `ops_recommendations` is healthy; it still reports drift for tables that DO exist. The `[FAIL] (WARN)` telemetry line no longer appears.
- [ ] #2: `terraform-apply-sandbox` `Terraform plan` step completes with no "No value for required variable"; the run reaches the deterministic guard; an empty required secret fails the run BEFORE plan.
- [ ] #3: a `ci-rca` run (dispatched on a known-failed run id) fetches the failed logs and files exactly one rec with `source="ci_rca"`, visible in preflight "CI RCA Recs (open)".
- [ ] #4: a second `ci-rca` trigger for the same `{workflow}+{head_sha}` (without `force_rca`) skips the `claude -p` step and files no duplicate; commit-status `ci-rca/<workflow>` is present on the head sha.
- [ ] No edit to `scripts/validate.py`'s check set; mypy stays informational; no `scripts/ops_writer.py` / `scripts/aws_profile.py` change (resolve_aws_profile already handles runner creds).
- [ ] rec-657 updated to resolved.

## Verification Plan
| # | Phase | Action | Command | Expected Outcome | Fix If |
|---|-------|--------|---------|-------------------|--------|
| 1 | [pre-deploy] | #1a passes under the runner condition (no `agent_platform` profile) | `AWS_CONFIG_FILE=/dev/null AWS_SHARED_CREDENTIALS_FILE=/dev/null AWS_PROFILE= bin/venv-python -m pytest tests/test_session_postflight.py::TestAutoMode::test_auto_recommendation_sync_in_output -q` | 1 passed | Test still asserts `drain_pending` 0 calls -> the `check_sso` mock target is wrong or drain/sync not mocked hermetically |
| 2 | [pre-deploy] | #1a does not make real AWS calls (hermetic) | `bin/venv-python -m pytest tests/test_session_postflight.py -q` | all pass, no network hang | `drain_pending_decisions`/`sync` unmocked -> real boto3 call; add the missing patches |
| 3 | [pre-deploy] | #1b: missing telemetry tables -> PASS, existing-table drift still flagged | `bin/venv-python -m pytest tests/test_verifiers/test_schema_integrity.py -q` | all pass incl. new skip-not-found case | Verifier still appends drift for a `None`/`{}` schema; the `if not remote_cols:` branch must `continue` without appending |
| 4 | [pre-deploy] | Full validate green (the real merge gate) -- "Unit tests + coverage" no longer in Failed checks | `bin/venv-python -m scripts.validate 2>&1 \| tail -30` | `Validation Summary` lists no Failed checks (or only pre-existing infra-skips); exit 0 | A different check fails -> diagnose that check; do NOT mask telemetry WARN as the cause |
| 5 | [pre-deploy] | Confirm telemetry verifier no longer emits a FAIL line | `bin/venv-python -m scripts.data_quality_runner 2>&1 \| grep -i "SchemaIntegrity"` | line shows PASS or SKIP, not `[FAIL]` for telemetry tables | Still FAIL -> the skip branch isn't reached for the not-found case |
| 6 | [post-deploy] | #2: trigger sandbox apply (write-PAT or human Actions-UI dispatch), then read result. Trigger: `curl -sS -X POST -H "Authorization: Bearer $(cat ~/.config/gh-mcp/token)" -H "Accept: application/vnd.github+json" https://api.github.com/repos/benjamin-blake/agent-platform/actions/workflows/terraform-apply-sandbox.yml/dispatches -d '{"ref":"main"}'` (if 403, human clicks Run workflow). Assert: `TOKEN=$(cat ~/.config/gh-mcp/token); RID=$(curl -sS -H "Authorization: Bearer $TOKEN" "https://api.github.com/repos/benjamin-blake/agent-platform/actions/workflows/terraform-apply-sandbox.yml/runs?per_page=1" \| jq -r '.workflow_runs[0].id'); curl -sS -H "Authorization: Bearer $TOKEN" "https://api.github.com/repos/benjamin-blake/agent-platform/actions/runs/$RID/jobs" \| jq -r '.jobs[].steps[] \| select(.name=="Terraform plan") \| .conclusion'` | `success` | `No value for required variable` -> the `TF_VAR_*` env wiring is wrong or a secret/var is missing (check the human prerequisite) |
| 7 | [post-deploy] | #3: dispatch ci-rca on a known-failed run with force, confirm a rec lands. Trigger: `curl -sS -X POST -H "Authorization: Bearer $(cat ~/.config/gh-mcp/token)" -H "Accept: application/vnd.github+json" https://api.github.com/repos/benjamin-blake/agent-platform/actions/workflows/ci-rca.yml/dispatches -d '{"ref":"main","inputs":{"run_id":"26710594834","force_rca":"true"}}'` (or human dispatch). Assert after completion: `bin/venv-python -m scripts.sync_ops pull >/dev/null 2>&1; bin/venv-python -m scripts.session_preflight 2>&1 \| grep -A3 "CI RCA Recs"` | at least one `ci_rca` rec listed (was "(none)") | ci-rca run "success" but no rec -> read its job log for the `claude -p` result; agent still blocked on logs (allowedTools) or `file_rec` errored |
| 8 | [post-deploy] | #4: re-dispatch ci-rca for the SAME run id WITHOUT force; confirm the agent step is skipped and no duplicate rec. Trigger: same as #7 with `"force_rca":"false"`. Assert: `TOKEN=$(cat ~/.config/gh-mcp/token); RID=$(curl -sS -H "Authorization: Bearer $TOKEN" "https://api.github.com/repos/benjamin-blake/agent-platform/actions/workflows/ci-rca.yml/runs?per_page=1" \| jq -r '.workflow_runs[0].id'); curl -sS -H "Authorization: Bearer $TOKEN" "https://api.github.com/repos/benjamin-blake/agent-platform/actions/runs/$RID/jobs" \| jq -r '.jobs[].steps[] \| select(.name \| test("ci-rca agent")) \| .conclusion'` | `skipped` | agent ran anyway -> the dedup `if:` gate or commit-status lookup is wrong |
| 9 | [post-deploy] | #4: confirm the dedup marker exists on the head sha | `TOKEN=$(cat ~/.config/gh-mcp/token); SHA=$(curl -sS -H "Authorization: Bearer $TOKEN" "https://api.github.com/repos/benjamin-blake/agent-platform/actions/runs/26710594834" \| jq -r .head_sha); curl -sS -H "Authorization: Bearer $TOKEN" "https://api.github.com/repos/benjamin-blake/agent-platform/commits/$SHA/statuses" \| jq -r '.[].context' \| grep ci-rca` | a `ci-rca/...` context present | marker never set -> the "Mark failure as RCA'd" step missing `statuses: write` perm or didn't run on success |

## Constraints
- Single-source-of-truth: never add a check to `.github/workflows/ci.yml` without adding it to `validate.py` first. This plan adds NO new checks; it fixes a failing test and a verifier branch.
- No rescue agents or workaround loops (Decision 55). The ci-rca agent remains diagnosis-only: it files a rec, never an autonomous fix.
- Fail-closed terraform guard (Decision 77): var injection precedes `terraform plan`; the guard keeps no `continue-on-error`; a missing secret reds the run, never skips the guard.
- T1.13 boundary: #3/#4 are operational repair only -- no typed RCA schema, no `scripts/ci_rca_evidence.py`, no taxonomy YAML, no strict-mode flag. The `ci-rca.md` edit is limited to the Step-1 log-fetch method.
- T2.15 boundary: #1b edits only the not-found branch of `scripts/verifiers/schema_integrity.py`; it does NOT touch `causal_chain.py`, `verifiers/__init__.py`, or `config/agent/data_quality/ops.yaml`, and does NOT delete tables from `MODEL_MAP`.
- Branch hygiene: all edits on the harness branch; never on `main`.

## Context
- **Root-cause evidence (from GitHub Actions logs, this session):**
  - #1: Both push-CI (#30) and Canary (#18) `Validation Summary` = `Failed checks: - Unit tests + coverage`. The single failing test is `test_session_postflight.py::test_auto_recommendation_sync_in_output`. The telemetry `SchemaIntegrityVerifier` fails at `WARN` severity and does NOT fail the gate -- so dropping telemetry tests would have left `main` red. The real cause: `run_auto` gates the outbox drain on `check_sso(_SSO_PROFILE)` (`scripts/session_postflight.py:673`); the runner has no `agent_platform` profile -> `check_sso` False -> drain skipped -> `mock_drain.assert_called_once()` fails. `check_sso` is a legacy name; under the static-key model it just probes whether the profile returns an identity (a credential-availability check, NOT SSO). Mocking it True exercises the credentials-available branch, exactly as `TestCheckSsoGating` already does.
  - #2: terraform plan failed with `No value for required variable` x4 (`account_id`, `owner_email`, `platform_dev_external_id`, `platform_admin_external_id`); the workflow supplied only `TF_VAR_aws_profile=""`.
  - #3: the ci-rca agent runs (~$0.39/run) but Step 1 `gh run view --log-failed` is denied (headless `claude -p` has no `--allowedTools` for `gh`; `GH_TOKEN` unset in that step). It never reaches `file_rec`, so 0 recs filed. `AWS_PROFILE=agent_platform` is set but is NEUTRALISED at runtime by `resolve_aws_profile` (`scripts/aws_profile.py:32`), which returns `None` when `AWS_ACCESS_KEY_ID` (OIDC) is present -- so it is a latent/confusing risk, removed defensively, not the blocker. No `ops_writer`/`aws_profile` change needed.
  - #4: `Main Canary` cron is `0 */3 * * *` (8/day); each identical failure spawns a ci-rca run -> ~$94/mo for zero output once you include the wasted Claude spend, plus duplicate recs once filing works. Dedup keyed by `{workflow}+{head_sha}` via a commit-status marker skips the expensive `claude -p` call on repeats; `workflow_dispatch` with `force_rca` is the escape hatch (Decision 74 precedent).
- **Decisions to cite:** Decision 77 (sandbox auto-apply behind fail-closed guard), Decision 72 (RCA-as-Plan-Source -- the mechanism #3/#4 repair), Decision 73 (two-tier forward-fix; sandbox tolerates red main; the canary is the L8 drift canary), Decision 55 (RCA files a rec, no inline fix), Decision 74 (ci-rca workflow self-heal + `workflow_dispatch` escape hatch -- direct prior art).
- **Why this work is in-policy despite Decision 72/55** ("do not hot-patch a red build outside the rec->/plan loop"): this IS the `/plan` review those decisions mandate; #1 is already triaged as rec-657; and the ci-rca agent is itself broken (files nothing), so "wait for the rec" would deadlock -- repairing the diagnostic harness is the Decision 55 "fix the gap permanently" pattern, with Decision 74 as precedent for fixing ci-rca via a normal plan.
- **Preflight at planning time:** `ci_rca_recs: []` (none open -> no HARD BLOCK; consistent with the broken filing path). `non_automatable_softcap_breached: true` (268 > 250; context note only). Branch 0 behind/ahead of `main`.
- **Verification access constraint:** the container PAT (`~/.config/gh-mcp/token`) is read-only (per `bin/setup-cloud-env.sh`), so `workflow_dispatch` POSTs may 403. VP steps 6-8 then require a human-initiated dispatch from the Actions UI; the implement agent verifies via the read-only GET API (which works) and via Athena (rec landed). Allow up to one canary cycle (<=3h) as a fallback natural trigger for #3/#4 if no dispatch is available.
- **Gotcha:** the `agent_platform`-profile-not-found lines in the validate pytest output come from inside test fixtures, not the real workflow runtime -- do not "fix" them by touching credential code.
- **Plan-critique gate: PROCEED** (fresh-context, all six scope files + `resolve_aws_profile`/`check_sso`/`variables.tf` read against `origin/main`; branch 0 behind). Five watch-items were raised (none REVISE) and folded into the steps above: (1) #1b also skips the not-found *exception* path, not just falsy returns; (2) the ci-rca `--allowedTools` glob and the agent's literal portal command must be the SAME binary path or `file_rec` is denied; (3) #1a's extra patches are hermeticity-only; (4) **known V3 limitation** -- under the read-only container PAT, VP6-9's *triggers* (`workflow_dispatch` POST) require a human Actions-UI dispatch or a natural canary cycle; the implement agent GET-verifies outcomes + Athena rec-landing autonomously, so post-merge verification is human-assisted, not agent-autonomous; (5) #4's marker step is gated on the agent step's own outcome so a downstream cosmetic failure cannot drop it.

## Pre-Implementation Checklist
- [ ] Branch confirmed not on `main`
- [ ] docs/PROJECT_CONTEXT.md read
- [ ] DECISIONS.md read (esp. 72, 73, 74, 77, 55)
- [ ] All files in Scope table located and readable
- [ ] Human prerequisite understood: the 2 new secrets + `OWNER_EMAIL` var must exist before #2 verification
- [ ] Acceptance Criteria understood and verifiable

## Ordered Execution Steps
1. **#1a (green main first)** -- `tests/test_session_postflight.py`, `test_auto_recommendation_sync_in_output`: add `patch("scripts.sync_ops.check_sso", return_value=True)` to the `with` block, and (for hermeticity, matching `TestCheckSsoGating`) `patch("scripts.ops_data_portal.drain_pending_decisions", return_value={"drained": 0})` and `patch("scripts.ops_data_portal.sync")`. Keep the existing `drain_pending` mock + `assert_called_once()`. Note (per critique): the `check_sso` mock is what flips pass/fail; the `drain_pending_decisions`/`sync` patches are purely hermeticity/hang-avoidance -- `run_auto` wraps both calls in `try/except BLE001` (`session_postflight.py:690-706`) so unmocked they would not fail the assertion but WOULD attempt real DynamoDB/Athena I/O (the autouse fixture patches `scripts.sync_ops.sync`, which does NOT cover `run_auto`'s `ops_data_portal.sync()` call). Run VP step 1-2.
2. **#1b** -- `scripts/verifiers/schema_integrity.py`: in the `if not remote_cols:` branch (~line 85-87), `continue` WITHOUT appending to `drift_reports` (a not-yet-provisioned table is "nothing to compare", not drift). Add a one-line comment citing the self-heal rationale. Tables that exist but mismatch are still reported. **Also (per critique) cover the raise-not-falsy case:** awswrangler may *raise* an entity/table-not-found error rather than return falsy; in the existing `except` handler (L131-139), when the error string indicates the table/entity is not found (e.g. `EntityNotFoundException`, `not found`, `does not exist`), `continue` (skip) instead of appending `"Error during comparison"`. Keep the credential-error -> `SKIPPED` branch (L133-138) unchanged. Run VP step 5.
3. **#1b coverage** -- `tests/test_verifiers/test_schema_integrity.py`: add/extend a test that mocks `wr.catalog.get_table_types` to return `{}` for telemetry tables and a valid schema for `ops_recommendations`, asserting `VerifierStatus.PASS` and no telemetry drift string; keep an existing-table drift case asserting FAIL(WARN). **Also cover the raise-path** from step 2: mock `get_table_types` to raise an entity-not-found error for a telemetry table and assert it is skipped (PASS, no drift string), and keep a credential-error case asserting `SKIPPED`. Run VP step 3, then VP step 4 (full validate green).
4. **Resolve rec-657** -- `bin/venv-python -m scripts.ops_data_portal update_rec rec-657 ...` to mark resolved, referencing this plan + the #1a fix.
5. **#2** -- `.github/workflows/terraform-apply-sandbox.yml`: add to the job `env:` block `TF_VAR_account_id: ${{ vars.AWS_ACCOUNT_ID }}`, `TF_VAR_owner_email: ${{ vars.OWNER_EMAIL }}`, `TF_VAR_platform_dev_external_id: ${{ secrets.TF_VAR_PLATFORM_DEV_EXTERNAL_ID }}`, `TF_VAR_platform_admin_external_id: ${{ secrets.TF_VAR_PLATFORM_ADMIN_EXTERNAL_ID }}`. Insert a fail-closed assert step immediately BEFORE `Terraform plan`: `: "${TF_VAR_account_id:?}" "${TF_VAR_owner_email:?}" "${TF_VAR_platform_dev_external_id:?}" "${TF_VAR_platform_admin_external_id:?}"`. Add a comment block naming the 2 required secrets + `OWNER_EMAIL` var (collocated documentation). Do NOT touch the guard/review/apply steps.
6. **#3** -- `.github/workflows/ci-rca.yml`: (a) add `actions: read` and `statuses: write` to the `rca` job `permissions`; (b) insert a "Fetch failed run logs" step before the agent step that resolves `RUN_ID` from `github.event.workflow_run.id || github.event.inputs.run_id`, sets `GH_TOKEN: ${{ github.token }}`, and runs `gh run view "$RUN_ID" --repo "${{ github.repository }}" --log-failed > /tmp/ci-rca-failed.log 2>&1 || gh run view "$RUN_ID" --repo "${{ github.repository }}" --log > /tmp/ci-rca-failed.log 2>&1 || true` (gh is pre-installed on ubuntu-latest); (c) in the "Run ci-rca agent" step, REMOVE `AWS_PROFILE: agent_platform`, add `--allowedTools` covering `Read`, `Bash(grep:*)`, `Bash(cat:*)`, and the ops-portal command, set `GH_TOKEN`, and change the prompt to point the agent at `/tmp/ci-rca-failed.log`. **Critical (per critique -- latent re-break):** the `--allowedTools` portal glob MUST be the exact literal the agent invokes, or `file_rec` is denied. Standardise ALL of {the agent prompt, `ci-rca.md` Steps 4-5, the `--allowedTools` glob} on ONE literal -- recommend the repo-standard `bin/venv-python -m scripts.ops_data_portal` (AGENTS.md), and ensure the "Activate venv" step builds `.venv` first so the wrapper resolves; do NOT leave the current `.venv/bin/python` in `ci-rca.md` while globbing `bin/venv-python` (or vice-versa). One glob `Bash(bin/venv-python -m scripts.ops_data_portal:*)` covers both the `get_rec_write_guidance` (Step 4) and `file_rec` (Step 5) subcommands.
7. **#3 agent instructions** -- `.claude/agents/scheduled/ci-rca.md`: replace the Step-1 `gh run view <run-id> --log-failed` instruction with "Read the pre-fetched failed-run log at `/tmp/ci-rca-failed.log` (provided in the prompt)." If standardising on `bin/venv-python` (step 6c), update the Steps 4-5 portal invocations from `.venv/bin/python` to `bin/venv-python` so they match the `--allowedTools` glob. Leave classification/evidence/`file_rec` semantics otherwise unchanged (T1.13 boundary -- no typed schema/evidence bundle).
8. **#4** -- `.github/workflows/ci-rca.yml`: add a `force_rca` boolean input to `workflow_dispatch`. Add a "Dedup guard" step (id `dedup`) before the fetch+agent steps that resolves `HEAD_SHA` (from `github.event.workflow_run.head_sha`, else `gh api .../runs/$RUN_ID --jq .head_sha`), builds context `ci-rca/<workflow-name-slug>`, and reads `gh api repos/.../commits/$HEAD_SHA/statuses` for that context's state -> output `already_filed`. Gate the fetch + agent steps with `if: github.event.inputs.force_rca == 'true' || steps.dedup.outputs.already_filed != 'true'`. Add a "Mark failure as RCA'd" step that POSTs the `ci-rca/<workflow>` commit status `state=success` on `HEAD_SHA`. **Per critique -- marker ordering:** gate this step on the AGENT step's own outcome (`if: steps.<agent-id>.outcome == 'success'`), give the agent step an `id`, and place the marker step IMMEDIATELY AFTER the agent step (before the cosmetic "Comment on originating PR" step). This ensures the marker is set whenever the agent actually filed, independent of any later non-RCA step failing -- so a downstream cosmetic failure cannot drop the marker and cause a duplicate re-diagnosis on the next trigger. (Fail-open toward re-diagnosis is acceptable per Decision 74, but dropping the marker after a successful file wastes a Claude run.)
9. **Execute Verification Plan** -- run VP steps 1-5 locally (gate green-main); commit/merge; then VP steps 6-9 via dispatch (write-PAT or human Actions-UI). Loop until pass. If a V3 step fails unrecoverably, stop and analyse root cause (Decision 55) -- do not loop-patch.
10. Report: what was implemented, the validate-green proof (VP4), the filed-rec id (VP7), and the dedup-skip proof (VP8).
