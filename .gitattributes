# Append-only JSONL logs: use union merge to keep all lines from both
# sides instead of inserting conflict markers. Correct because each line
# is an independent JSON record and order is not semantically significant.
*.jsonl merge=union
