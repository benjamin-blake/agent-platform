---
# Graduated verification check registry.
# Governs: docs/contracts/verification-registry.yaml (Class A, contract_version: 1).
# Each entry records a check admitted by the differential admission gate (T3.1 / CD.29).
# Validated by validate_verification_registry() in scripts/validate.py.
entries: []
