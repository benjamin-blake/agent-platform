# config/lambda/ops-compaction/

Per-Lambda runtime payloads for the `ops-compaction` Lambda function. Files here are bundled
into `ops-compaction.zip` by `scripts/build_lambda.py`. Initially empty -- populated as
Lambda-specific config emerges.
