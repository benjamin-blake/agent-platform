# Single source of truth for the DuckDB/DuckLake lockstep version (OQ.12).
# TO BUMP: edit ONLY duckdb_version here, then run:
#   bin/venv-python -m scripts.sync_ducklake_version   (syncs requirements.txt floor)
#   bin/venv-python -m scripts.build_lambda --ducklake-only  (rebuilds layers + function zips)
# The ducklake-version-lockstep validate gate enforces that no derive surface diverges from this value.
duckdb_version: "1.5.4"
ducklake_format: "v1.0"
extension_platform: "linux_amd64"
