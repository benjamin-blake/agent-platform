# config/lambda/data-pipeline/

Per-Lambda runtime payloads for the `data-pipeline` Lambda function. Files here are bundled
into `data-pipeline.zip` by `scripts/build_lambda.py`. Initially empty -- populated as
Lambda-specific config emerges.
