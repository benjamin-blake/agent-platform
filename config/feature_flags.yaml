# Feature flags for the agent platform.
# Read at process start by scripts/ops_data_portal.get_ci_rca_strict_mode().
# CI_RCA_STRICT_MODE: warn | strict
#   warn   -- structural deficiencies log a warning; the rec is still filed.
#   strict -- structural deficiencies raise ValueError (inert until Phase 4 back-validation).
CI_RCA_STRICT_MODE: warn
