"""Tests for src/common/ducklake_runtime.py (T2.17 / CD.33, 100% coverage).

DECOMPOSED (rec-2709 Wave 7): every test previously here has moved to the
tests/common/test_ducklake_{writes,tables,reads,metrics,runtime}.py mirror modules (Decision 131).
This file is retained EMPTY as the still-grandfathered home (see
scripts/test_coverage_checker.py's _RETIRING_GRANDFATHER_HOMES / _DUCKLAKE_RUNTIME_SPLIT_MODULES)
until the FINAL commit of this wave deletes it and flips the map to the mirror.
"""

from __future__ import annotations
