"""Tests for scripts/ducklake_neon_smoke_test.py (T2.16b / CD.34 DuckLake Neon smoke test).

DECOMPOSED (rec-2709 Wave 7): every test previously here has moved to the
tests/ducklake_neon_smoke_test/test_*.py concern-split package (Decision 131), following the
scripts/ducklake_smoke/ submodule structure. This file is retained EMPTY as the still-grandfathered
home (see scripts/test_coverage_checker.py's _RETIRING_GRANDFATHER_HOMES /
_CONCERN_SPLIT_TEST_PACKAGES) until the FINAL commit of this wave deletes it and flips the map to
the concern-split package.
"""

from __future__ import annotations
