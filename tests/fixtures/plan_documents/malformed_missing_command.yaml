schema_version: 1
slug: zz-malformed-demo
intent: Malformed fixture -- verification step 1 has a whitespace-only command (must fail validation).
plan_type: IMPLEMENTATION
verification_tier: V2
plan_path: docs/plans/PLAN-zz-malformed-demo.yaml
phase: Test fixture -- no roadmap phase.
scope:
  - file: scripts/example.py
    action: Create
    purpose: Demonstration scope entry.
acceptance_criteria:
  - Example acceptance criterion.
verification_plan:
  - step: 1
    phase: pre-deploy
    action: Run the example check
    command: "   "
    expected: prints ok
    fix_if: never fails in practice
execution_steps:
  - Create scripts/example.py.
