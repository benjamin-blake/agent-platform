schema_version: 1
slug: zz-valid-demo
intent: Demonstration fixture exercising every PlanDocument field (T1.11 test asset).
plan_type: IMPLEMENTATION
verification_tier: V2
plan_path: docs/plans/PLAN-zz-valid-demo.yaml
phase: Test fixture -- no roadmap phase.
scope:
  - file: scripts/example.py
    action: Create
    purpose: Demonstration scope entry.
bundled_recommendations:
  - "rec-000: example bundled recommendation"
infrastructure_dependencies: []
acceptance_criteria:
  - Example acceptance criterion.
verification_plan:
  - step: 1
    phase: pre-deploy
    action: Run the example check
    command: echo ok
    expected: prints ok
    fix_if: never fails in practice
constraints:
  - Example constraint.
context:
  - Example context line.
pre_implementation_checklist:
  - Example checklist line.
execution_steps:
  - Create scripts/example.py.
rollback: git revert the implementing commit
