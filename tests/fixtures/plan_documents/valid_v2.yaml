schema_version: 2
slug: zz-valid-v2-demo
intent: Demonstration schema_version-2 fixture exercising the closed phase enum (T3.17 test asset).
plan_type: IMPLEMENTATION
verification_tier: V2
plan_path: docs/plans/PLAN-zz-valid-v2-demo.yaml
phase: Test fixture -- no roadmap phase.
scope:
  - file: scripts/example.py
    action: Create
    purpose: Demonstration scope entry (Python source, floors to V2).
bundled_recommendations: []
infrastructure_dependencies: []
acceptance_criteria:
  - Example acceptance criterion.
verification_plan:
  - step: 1
    phase: pre-deploy
    action: Run the example check
    command: echo ok
    expected: prints ok
    fix_if: never fails in practice
    hermetic: true
  - step: 2
    phase: post-deploy
    action: Invoke the deployed example
    command: echo deployed
    expected: prints deployed
    fix_if: never fails in practice
constraints:
  - Example constraint.
context:
  - Example context line.
pre_implementation_checklist:
  - Example checklist line.
execution_steps:
  - Create scripts/example.py.
rollback: git revert the implementing commit
