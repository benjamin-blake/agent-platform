# Fast-tier subset for CI pr-validate job.
# Versions are constrained by requirements.txt via -c; only package names are listed
# here so requirements.txt is the single source of truth for all version pins.
-c requirements.txt
ruff
mypy
pytest
pytest-picked
pyyaml
pydantic
