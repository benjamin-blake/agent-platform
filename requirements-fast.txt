# Fast-tier subset for CI pr-validate job (ruff, mypy, pytest --picked, prompt validators).
# Version constraints must match requirements.txt; Dependabot updates both files in the
# same grouped PR so they stay in sync automatically.
ruff==0.15.20
mypy>=1.5.0
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-picked>=0.5.1
pytest-randomly>=4.1.0
pytest-socket>=0.8.0
pytest-xdist>=3.6.1
pytest-timeout>=2.3.1
pyyaml>=6.0.3
pydantic>=2.0.0
networkx>=3.6.1
import-linter>=2.0
